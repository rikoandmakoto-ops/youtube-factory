# メモリ更新差分 — 2026-09-05（youtube-orchestrator 定時実行）

実行: 2026-09-05 10:05–10:45 JST
出力: `reports/youtube_analysis_20260905.xlsx`（5シート）

> ⛔ `~/.auto-memory/` は依然として接続フォルダ外（8日連続）。本ファイルと `HANDOFF.md` を
> YouTube Factory のメモリ本体として扱う。恒久対応は Cowork の接続フォルダに `~/.auto-memory` を追加すること。

---

## 0. ★★★ 最重要 — `video_metrics` の量的指標は「累積カウンタ」である

**これを今日まで誤読していた。過去の日次レポートの再生数・登録者・表示回数の週次比較は、
おそらく全て間違っている。**

同一動画の `views` の実測推移:

```
08-11: 775 → 08-12: 940 → 08-13: 940 → 08-15: 940 → 08-18: 941 → 08-21: 937 → 09-04: 939
```

単調に積み上がる**累積値**で、その日の増分ではない。`impressions` / `likes` /
`subscribers_gained` も同じ。

つまり `SELECT SUM(views) ... WHERE date BETWEEN a AND b` は
**「累積値 × スナップショット日数」**を足しているだけで、実績ではない。
取得回数が減った日があれば、それがそのまま「実績の落ち込み」に化ける。

> 今週は 09-01 が48行、09-02 が16行しか取れていない（通常は約210行）。
> この欠損を含んだ7日合計で前週と比べると **-11.5%** という数字が出る。
> **これは実績ではなく取得回数の差。** 一度この数字でレポートを書きかけて、検証で気づいた。

**正しい算出方法（今後これで固定する）**: 各動画について
「週末時点の最新スナップショットの累積値 − 週初時点の最新スナップショットの累積値」を取り、それを合計する。

---

## 1. ★★ 正しく測ると、再生数は伸びている

累積差分で出し直した週次の**実増分**（08-28→09-04 vs 08-21→08-28）:

| ch | 今週Δ再生 | 前週Δ再生 | 比 | 今週Δ登録 |
|---|---|---|---|---|
| scp-lab | 11,415 | 6,695 | **1.71** | 11 |
| yokai-watch | 5,120 | 4,613 | 1.11 | 2 |
| pokemon-lab | 7,796 | 7,049 | 1.11 | 1 |
| daily-science | 7,446 | 7,311 | 1.02 | 8 |
| **2ch-matome** | 8,995 | 10,346 | **0.87** | 2 |
| fake-paper | 4,077 | 1,099 | 3.71 | 0 |
| **既存6ch 合計** | **44,849** | **37,113** | **1.208** | **24（前週11）** |

**再生数 +20.8%、登録者の実増分も 11 → 24 で倍増。量は問題ではない。**

> `company-facts` / `akashic-librarian` / `clip系` は **video_metrics が 2026-09-01 以降にしか無い**
> （`SELECT MIN(date) ... GROUP BY channel_id` で確認）。前週側に行が無いので比較不可。
> **全社合計の前週比を出してはいけない。**新規ch分がまるごと「成長」に化ける。

---

## 2. ★★★ ボトルネックは維持率。主要5ch全てが下がり、company-facts だけ上がった

公開週コホート（各動画の最新スナップショット / `views>0` / `avg_view_percentage BETWEEN 0 AND 100` / n>=3）。
**この母集団条件を本レポート全体で1種類に統一した。今後もこれを使うこと。**

| ch | W32 | W33 | W34 | W32→W34 | 平均視聴秒 W32→W34 |
|---|---|---|---|---|---|
| pokemon-lab | 55.7% | 40.4% | **28.1%** | **-27.6pt** | 16.5→15.6秒 |
| 2ch-matome | 59.2% | 44.3% | **37.6%** | **-21.6pt** | 12.8→13.2秒 |
| yokai-watch | 61.6% | 35.1% | **41.1%** | **-20.5pt** | 17.8→17.1秒 |
| scp-lab | 48.9% | 33.8% | **32.7%** | -16.2pt | 16.9→14.3秒 |
| daily-science | 46.6% | 37.3% | **41.2%** | -5.4pt | 13.3→17.5秒 |
| **company-facts** | 39.8% | 51.1% | **63.6%** | **+23.7pt** | **32.5→35.2秒** |

平均再生も pokemon-lab 1253→795（-37%）、yokai-watch 928→635（-32%）に対し
company-facts は 1026→1730（+69%）。

> **08-23 の style_rules #2「推定尺26.0秒の短い側が35.8秒の長い側より登録者1.33倍」は
> W33以降のデータで再現していない。**
> ただし #2 の分析は「維持率45〜70%の動画に限って比較」しており、処置後変数で層別した比較なので
> どちらも決定打ではない。**断定せず対照実験に切り替えた**（§4-3）。

---

## 3. ★★ 崖は再生位置 5%→10%（≒1.5〜3秒）。company-facts だけ平坦

`retention_curve` の `relative_retention`（YouTube同種動画比、0.5が中央値）を ch ごとに直近25本平均。

| ch | 5% | 10% | 落差 |
|---|---|---|---|
| **company-facts** | 45.8% | 44.0% | **-1.7pt** |
| clip-fukada | 53.6% | 45.1% | -8.4pt |
| clip-lab | 43.3% | 38.5% | -4.8pt |
| daily-science | 34.7% | 30.9% | -3.8pt |
| pokemon-lab | 33.4% | 30.4% | -3.0pt |
| yokai-watch | 33.3% | 30.2% | -3.0pt |
| **scp-lab** | 32.3% | 27.3% | **-5.0pt** |
| **2ch-matome** | 29.4% | 25.5% | **-3.9pt** |

崖の位置は**全12chで例外なく 5%→10%**。company-facts だけ落差が小さく、かつ絶対値も最高。

**company-facts と他chの構造的な差は2つだけ:**
1. 冒頭で数字を画面に大きく出す（`facts_overlay`、`font_size_main=96`）
2. `slideshow.max_images=6` + `switch_per_fact=true`（ゆっくり5chは `short_illustrations.max_count=2`）

この2点を全chへ移植したのが本日の主施策。

---

## 4. 本日 反映した変更（12件。詳細は xlsx「改善アクション」シート）

### 反映済み・即日効く（`style_rules` と `max_count` は実行ごとに読み直される）

1. **全6ch**: `style_rules` に「冒頭3秒フック」ルール。1行目に出典のある数字＋単位、そのテロップを最大サイズで表示、挨拶・前置きは不合格、数字の創作は禁止。
2. **ゆっくり5ch**: `video_format.short_illustrations.max_count` を **2 → 4**。旧値は `_max_count_prev_20260905` に保存。挿絵は 5%/10%/20%/40% 地点の直前に置くよう指示。
3. **尺の対照実験を開始**（`_experiments.length_20260905`）
   - **実験群 = scp-lab / 2ch-matome**: 目標尺 32〜36秒。この実験ルールが 08-23 ルール#2 に優先する。
   - **対照群 = daily-science / pokemon-lab / yokai-watch**: 現行のまま。**09-12まで尺に触れてはいけない。**
   - **評価日 2026-09-12**。09-05〜09-09 公開分の `avg_view_percentage`(0-100) と `subscribers_gained/1000views` を両群で比較。実験群が悪化したらルールを削除して #2 に戻す。
4. **全6ch**: 「正体」ルールに上限。(A-1) 1バッチ最大1本 (A-2) 不自然なら入れない (A-3) 数字＋単位の要件は維持。
5. **company-facts**: 同一企業の数値矛盾を防ぐルール（出典・年度の明示義務）。あわせて top-level `days_of_week` が `[1,3,4]` で `times`(全曜日) と食い違っていたのを是正。
6. **全6ch**: `analytics_policy.evaluation_lag`（`min_age_days=3`）を記録。
7. **コード**: `success_analyzer.py` に `_ret()`（0-100クランプ）を追加し、**判定・平均・LLMプロンプト・出力JSONの全経路**で使用。`posting_optimizer.py` の `retention` もクランプ。`thumbnail_ab_test._channel_avg_ctr` の母集団を `improvement_queue` と統一（`views>=100`・直近200行）し、1以上の値を単位崩れとして除外。全て `.bak_orch_20260905` あり。

### ファイルは書き換え済みだが **バックエンド再起動が必要**

8. **投稿枠**（`autopilot.schedule.times`）
   - 2ch-matome **18:00 → 21:00**
   - scp-lab **09:00 → 17:00**（平日・週末とも。旧式の top-level `hour=9` も 17 に揃えてフォールバック復活を防いだ）
   - yokai-watch **19:00 → 17:45**

> **投稿枠(cron)は起動時の `restore_all()` でしか再登録されない**（`api_channel_autopilot.py:502`）。

### 手元実行が必要

9. **サムネA/Bのベースライン修正**: `scripts/fix_thumbnail_ab_baseline_20260905.py`（既定ドライラン、`--apply` で書き込み）。
   サンドボックスから `analytics.db` に書くと `disk I/O error` になるため切り出した。

**→ 8と9はまとめて `apply_orchestrator_20260905.command` をダブルクリックすれば済む。**

---

## 5. 🐛 本日見つけたバグ3件（0番も含めると4件）

### 5-1. `thumbnail_ab_tests.channel_avg_ctr` が壊れていた

同じ daily-science の行に `0.029113044331873048` と `42.909021329774575`（scp-lab は `37.286`）が同居。
一方 `video_metrics.ctr` は **`ctr>0` の1,352行すべてが 0.0029〜0.3333 で、1以上の行は0件**。

> ⚠️ **42.909 が何のスケールなのかは特定できていない。**「百分率の遺物」では説明がつかない
> （0.0291 の百分率は 2.91 であって 42.9 ではない）。原因不明のまま、
> 「現行スキーマではあり得ない値」という事実だけを根拠に是正した。

切替判定が `last_check_ctr < channel_avg_ctr * 0.8` なので、ベースラインが壊れると判定も壊れる。
実際 **19件中17件が `status=monitoring` のまま放置、切替は2件だけ = サムネA/Bは事実上死んでいた。**

**DBを直すだけでは足りない。**書き込み側の `thumbnail_ab_test._channel_avg_ctr()` は
`views` フィルタ無し・直近50行という別条件だったので、そこも `improvement_queue` に揃えた（§4-7）。

### 5-2. `avg_view_percentage` に100超が200行（最大 3230.87%）

内訳: daily-science 100 / pokemon-lab 43 / 2ch-matome 30 / scp-lab 22 / yokai-watch 4 / fake-paper 1。
ショートのループ再生がそのまま積まれている（YouTube の仕様でありAPIのバグではない）。

問題は `success_analyzer.py` が**この生値で上位25%を選んで success と判定していた**こと。
つまり `success_patterns.json` は「ループ再生された動画」を勝ちパターンとして学習していた可能性が高い。
**次回 `success_patterns.json` が再生成されたら中身を確認すること。**

> これまでの日次レポートで daily-science の維持率が 97〜98% と出ていたのはこの外れ値の混入。
> **過去レポートの維持率の数字は信用しないこと。**

### 5-3. 09-04 のスナップショットが部分的に壊れている

09-04 取得分に **`views=0` かつ `impressions>0` の行が17件**ある（通常は1〜8件）。
また35動画で累積 `views` が前日より減っている（合計 -27,728）。
例: company-facts 08-21公開「⚠️三菱重工…」は 09-03 に views=28 / 視聴秒29 だったのが
09-04 に views=0 / impressions=215 / CTR4.19% / 維持率49.65% / 視聴秒0 という内部矛盾した行になっている。
**取得の信頼性そのものに問題がある。次回、取得後に整合チェックを入れることを検討。**

---

## 6. 品質の問題2件

### 6-1. 「正体」ルールが暴走している

出現率（主要6ch、公開日別）: 08-26〜08-31 は 0〜18% → **09-03 に 5/7本（71%）** → 09-04 は 1/13本（7%）
→ **09-05 午前も6本中4本**。「毎バッチ最低1本」が実運用で「ほぼ全本」に化けている。

結果として `🤣ワイ健康診断歴3年やけど正体あるw🍑` のような日本語として成立しないタイトルが出た。

さらに **09-03 の「4ch全てで同方向・1.53倍」という根拠は現行データで再現できない**。
08-15以降の成熟動画で「正体」入りは daily-science n=10 / yokai-watch n=4 の計14本しかなく、
2ch-matome / company-facts / pokemon-lab / scp-lab は **n=0**（そもそも1本も無い）。

### 6-2. company-facts で同一企業の年収が連日で食い違った

- 09-03 公開『日本マクドナルドの**正体**、年収**670万円**の裏側』
- 09-04 公開『日本マクドナルドの年収**576万円**、その内訳とは』

数字が売りのチャンネルで最も避けるべき矛盾。§4-5 で対処した。

---

## 7. 分析の作法として固定すること

1. **量的指標（views / impressions / likes / subscribers_gained）は累積カウンタ。週次は必ず累積差分で出す。**（§0）
2. **前週比は両期間に行があるchに絞る。**（§1）
3. **`avg_view_percentage` は必ず `BETWEEN 0 AND 100` でフィルタしてから平均する。**（§5-2）
4. **YouTube Analytics は2〜3日遅延する。** 09-02以降に公開した35本は全指標ゼロだが**不調ではなく未着信**。
   08-31公開は10本中8本にデータあり。**前日公開分の「再生数0」を不調と誤読しない。**評価は公開後3日以上。
5. **時間帯別は必ずチャンネル内で比較する。** 全社集計だと17時が最良（sub/1k=0.792）に見えるが、
   もともと17時に投稿していたのが daily-science / company-facts / pokemon-lab という
   登録効率の高い3chだったという交絡が入る。
6. **本文の数字と表の数字は同じクエリから出す。** 今日は最初、本文が `NULLIF(avp,0)`（ゼロ除外）、
   表が `BETWEEN 0 AND 100`（ゼロ込み）になっていて食い違った。

### ch内の時間帯別 実測（08-01以降、n>=3、sub/1k）

| ch | 実測 | 判断 |
|---|---|---|
| 2ch-matome | 09時 0.290(n=5) / 12時 0.381(n=5) / **18時 0.000(n=9)** / 21時 0.307(n=4) | 18時は9本で登録0人 → 21時へ |
| scp-lab | **09時 0.473(n=19)** / 12時 1.314(n=3) / 13時 0.575(n=4) / 18時 0.631(n=7) / 19時 1.080(n=12) | 最大サンプルの09時が最弱 → 17時へ |
| yokai-watch | 12時 0.537(n=7) / 18時 0.574(n=3) / **19時 0.218(n=18)** | 最大サンプルの19時が最弱 → 17:45へ |
| daily-science | 09時 0.202(n=6) / 13時 0.312(n=4) / **17時 0.848(n=18)** / 18時 0.228(n=6) | 17時が明確に強い（据え置き） |
| company-facts | 14時 0.383(n=6) / **17時 0.925(n=9)** / 19時 0.936(n=5) | 据え置き |
| pokemon-lab | 12時 0.269(n=5) / 17時 0.376(n=8) / 18時 0.280(n=16) | 差が小さく据え置き |

> ⚠️ **移設先の17時台は、そのchでの実績が無いので他chの実績を根拠にしている。ここだけch内比較になっていない。**
> ⚠️ **yokai-watch の19時は ch内で平均再生が最高（1273.5、n=18）の枠でもある。**
> 登録者を優先して手放した判断なので、09-19 には再生数が落ちていないかも併せて見ること。

---

## 8. 登録者効率（至上目標なのでここを見る）

W34 コホートの `subscribers_gained / 1000 views`:
daily-science 0.710 / company-facts 0.618 / scp-lab 0.577 / yokai-watch 0.478 / pokemon-lab 0.231 / 2ch-matome 0.200

今週の実増分ベースでも同じ傾向: **登録者Δは scp-lab 11 / daily-science 8 に対し、pokemon-lab 1 / 2ch-matome 2。**
（company-facts は Δ29 だが計測開始が09-01なので他chと同列に比べられない）

**pokemon-lab と 2ch-matome は再生を稼ぐが登録に繋がっていない**（合計で1日6枠を使っている）。
今日は枠の振り替えまではやっていない。**09-12 の実験評価と合わせて、この2chの枠配分を判断すること。**

## 9. 最大の単一改善余地: scp-lab のサムネイル

累積表示回数 **113,153**（全社の56%を1chで占める）に対し **CTR 1.54%**（全ch最下位、他主要5ch平均3.46%）。
今週の表示回数の実増分も **8,836 で全ch最大** — 露出は出ているのにクリックされていない。
§5-1 の A/B 修正を入れたうえで scp-lab を A/B 対象に増やすのが次の一手。

## 10. 本日のバックエンド稼働と既存の失敗

09-05 の autopilot は正常発火: 06:15 2ch-matome / 06:45 daily-science / 07:30 company-facts /
07:45 pokemon-lab / 08:15 scp-lab / 09:15 akashic-librarian、いずれも公開完了。
**自動投稿は動いているので手動トリガーは打っていない**（打つと予定外の増産になる）。

継続中の失敗（切り抜き系。指揮者の対象外だが枠が空く）:
- `clip-lab`: **`ANTHROPIC_API_KEY 未設定`** で viral エンジンが3回試行して失敗
- `clip-animal`: 全ての元動画が切り抜き済み（`clips_per_video` を上げるか元動画を足す）
- `clip-fukada`: 切り抜ける元動画が見つからない

## 11. テーマキュー残量（補充は不要だった）

daily-science 45 / 2ch-matome 45 / company-facts 49 / scp-lab 40 / yokai-watch 31 / pokemon-lab 29。
1日3本なので最短の pokemon-lab でも約10日分。**今日は補充していない。09-10 頃に再確認。**

---

## 12. 実行環境の制約（毎回同じなので回避策ごと残す）

| できないこと | 症状 | 回避策 |
|---|---|---|
| YouTube Data API の新規取得 | `googleapis.com` に到達できない | 既存の `analytics.db`（最終取得 09-04 23:12 JST）で分析 |
| `localhost:8000` への到達 | bash・内蔵ブラウザとも不可（`host.docker.internal` は403） | 手動トリガーは打たず APScheduler に任せる |
| `analytics.db` への書き込み | `sqlite3.OperationalError: disk I/O error`（読み取りも途中で失敗する） | `/tmp` にコピーして読み、更新は `.command` に切り出す |

> **`data/channels_orchestrator/*.json` は `../channels/*.json` へのシンボリックリンク**（`ls -li` で確認）。
> 実体は1つなので「両方に反映する」作業は不要。

### バックアップの状態（ロールバック時の注意）

`data/channels/*.json.bak_pdca_20260905` は主要6ch分あるが、
**2ch-matome / pokemon-lab / yokai-watch / company-facts の bak は 10:12 取得で施策①②が既に入った状態**。
完全な巻き戻しに使えるのは daily-science / scp-lab の bak（前夜23:41取得）のみ。
他4chを戻す場合は `_max_count_prev_20260905` の値と `style_rules` の該当行削除で手当てすること。

---

## 13. 次回（09-06）にやること

1. `apply_orchestrator_20260905.command` が実行済みか確認（未実行なら投稿枠3件とサムネA/B修正が未反映）
2. `success_patterns.json` の再生成結果を確認（§5-2 のクランプが効いているか）
3. 09-02〜09-03 公開分にデータが着信しているはずなので、**冒頭3秒フック導入前の基準値**として
   5%→10% 落差を記録しておく（09-12 の比較に必要）
4. §5-3 の取得破損（`views=0` かつ `impressions>0`、累積値の減少）が 09-05 分にも出ているか確認
5. scp-lab のサムネA/B を開始する
