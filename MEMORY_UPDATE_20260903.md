# メモリ更新差分 — 2026-09-03 朝（youtube-orchestrator 定時実行）

> `~/.auto-memory/` は接続フォルダ外のため**5日連続で直接書き込めていない**。
> 接続済みは `Developer` / `youtube-factory` / `iCloud Documents` / `Downloads` の4つ。
> **恒久対応: Cowork の接続フォルダに `~/.auto-memory` を追加する。**
> 当面は本ファイルと `youtube-factory/HANDOFF.md` を YouTube Factory のメモリ本体として扱う。

---

## 1. 🚨 OAuth ── 全13チャンネルが失効した（今日いちばん重要）

09-02 の予測は「正常な4chの残寿命が 4.62日 なので **09-07 前後**に全滅する」だった。
**実際は 09-02 15:00〜15:01 に既に失効していた。予測より5日早い。**

`data/youtube_tokens.db` の `expires_at` 実測（09-03 10:04 時点）:

| 失効時刻 | チャンネル |
|---|---|
| 08-31 05:07 | clip-fukada / clip-kaneko |
| 08-31 15:00 | daily-science |
| 09-01 03:04〜03:06 | 2ch-matome / clip-lab / company-facts / pokemon-lab / scp-lab / yokai-watch |
| 09-02 15:00〜15:01 | akashic-librarian / clip-animal / fake-paper / socio-rx |

**残寿命がプラスのチャンネルは1つも無い。** 再認可するまで、本日以降に生成される動画は
1本も公開されないし、Analytics API も叩けない。

- 恒久対策は変わらず **GCP 同意画面の「本番」公開**
  （https://console.cloud.google.com/auth/audience?project=844705815004 / project 844705815004）。
  テスト中である限りリフレッシュトークンは7日で失効し、**08-24・08-31・09-02 と既に3回再発している**。
  再認可だけを繰り返しても必ず4回目が来る。
- `backend/pipeline/credentials/client_secret.json` は依然として存在せず、
  アップロード経路が `FileNotFoundError` で落ち続けている点も未解決のまま。
- 実行用に `restart_and_trigger_20260903.command` を新規作成した（トークン診断 → 同意画面 →
  再認可 → 手動トリガの順に案内する）。

## 2. データ基盤 ── 実績取得が止まっている

`video_metrics` の最新スナップショットは **moviepy 6ch中5chが 08-31 で停止**
（2ch-matome / daily-science / pokemon-lab / scp-lab / yokai-watch）。
company-facts は 09-01、akashic-librarian と fake-paper のみ 09-02。
**停止時刻はトークン失効時刻と一致しており、原因は同一。** データ基盤の別障害ではない。

`channel_metrics` は 08-30 で停止（09-02 メモの「08-28〜30 で更新停止」から進んでいない）。

**この結果、09-02 に適用した speed 引き下げ（4ch 1.3→1.2 / 2ch-matome 1.35→1.25）は
変更後データが1件も無く、09-05 期限の反証判定ができない。** 期限は再認可後にデータが
揃うまで延長する。判断を AVP ではなく絶対視聴秒で行う点は据え置き。対照群も company-facts のまま。

## 3. ★ タイトル判断を全面的に見直した（08-23 のルールは交絡による誤りだった）

これが本日いちばん大きな分析上の発見。

**これまでのタイトル分析はチャンネルをまたいで あり/なし を比較していたため、
「どの語が効くか」ではなく「どのチャンネルが強いか」を測っていた。**
company-facts は登録/千再生 0.73 で 2ch-matome 0.16 の 4.6倍あり、company-facts に多い語は
何であれ強く見えてしまう。

そこで **同一チャンネル内でのみ あり/なし を比較**し直した（公開07-05以降・100再生以上・n=286）。

| 特徴 | 登録/千 有 | 無 | 効果量 | 判定 |
|---|---:|---:|---:|---|
| **「正体」** | 0.64 | 0.42 | **1.53倍** | **強く採用**（対照可能な4ch全てで同方向） |
| **数字＋単位** | 0.61 | 0.39 | **1.56倍** | **採用** |
| 「実は」 | 0.51 | 0.49 | 1.03倍 | 効果なし |
| 絵文字 | 0.48 | 0.47 | 1.03倍 | 効果なし |
| 「なぜ」 | 0.42 | 0.45 | 0.94倍 | 効果なし |
| 連番 #NN： | 0.40 | 0.48 | 0.84倍 | 負・禁止継続 |
| **「？」疑問形** | 0.37 | 0.60 | **0.62倍** | **負・必須化を撤回** |

**書き換えたメモリ項目（古い記述は削除すること）:**

- ❌「疑問符『？』は 2.16倍で最も効く。必ず疑問形にする」（08-23）
  → **逆。0.62倍で負。6ch中4chで負。本日必須化を撤回した。**
- ❌「『なぜ』始まりは 1.58倍で有効」（08-23）→ **再現せず。0.94倍＝効果なし。**
- ❌「『実は』は 0.70倍で逆効果につき使わない」（08-23）
  → **交絡。1.03倍＝効果なし。禁止を解除したが推奨もしない。**
- ❌「絵文字は company-facts で効いている」→ **交絡。1.03倍。**
- ✅「『正体』は 2.10倍」（08-23夜）→ **別コホートで 1.53倍として再現された唯一の語。**
  daily-science 2.31 / scp-lab 1.13 / pokemon-lab 1.74 / yokai-watch 2.73 と4ch全て同方向。
  **タイトル系の知見でこれだけが信頼できる。**
- ✅「連番プレフィックスは劣後」（08-31）→ **0.84倍で再現。停止判断は正しかった。**

> **教訓として残す: このプロジェクトのA/B判断は必ずチャンネル内対照で行う。
> チャンネル間比較は登録率が最大4.6倍違うため、ほぼ全ての語が有意に見えてしまう。**

## 4. 高評価率が登録の最強レバーである（08-29 の発見が再現）

高評価率4分位 → 登録/千再生（n=339・moviepy 6ch・公開06-01以降）:

| 四分位 | 高評価率 | 登録/千再生 |
|---|---|---:|
| Q1 | 0.00〜0.21% | 0.23 |
| Q2 | 0.21〜0.36% | 0.37 |
| Q3 | 0.37〜0.56% | 0.52 |
| Q4 | 0.56〜1.39% | **0.75** |

単調増加・**Q4はQ1の3.3倍**。08-29 に n=322 で見つけた法則が別コホートで再現した。
**終盤維持率でも尺でもなく高評価率が登録の観測レバーである**という結論は維持でよい。
6行目CTAは「高評価を先、登録を同じ行に」を全chの short_format 最上位ルールへ再掲した。

なお **CTA遵守率の効果測定（09-01 メモから3日連続で未実施）は今日も未実施**。
再認可後、`cta_enforcer.py` 適用前後の高評価率を突き合わせること。

## 5. 維持率 ── 離脱点は全ch共通、差は「後半の情報密度」

維持率カーブ（直近25本平均）で **6ch全てが再生位置15〜25%地点に最大離脱**（-0.15〜-0.21）。
ここは 09-02 に入れた「答えの遅延」の狙い通りの位置なので、施策の方向は合っている。

新しい発見は**底の深さ**のほう。50%地点の audience_watch_ratio:

- company-facts **0.62**
- pokemon-lab 0.43 / daily-science 0.39 / scp-lab 0.36 / 2ch-matome 0.34 / yokai-watch 0.33

company-facts だけ約1.7倍を保つ。台本を見ると差は「後半にも新しい数字が出続けるか」で、
他chは4行目をたとえ話だけで埋めて数字が途切れている。
→ **4行目・5行目にそれぞれ新しい具体数字を1つずつ置く**ルールを全chに追加した。

## 6. チャンネル序列（直近21日・公開分）

| ch | 本数 | 平均再生 | AVP | 平均視聴秒 | 登録/千再生 |
|---|---:|---:|---:|---:|---:|
| akashic-librarian | 8 | 450 | 50.7% | 17.8s | **0.83** |
| **company-facts** | 24 | 1,321 | 57.5% | **33.0s** | **0.73** |
| scp-lab | 30 | 577 | 36.9% | 14.4s | 0.58 |
| daily-science | 23 | 728 | 41.0% | 15.2s | 0.54 |
| pokemon-lab | 21 | 945 | 40.8% | 16.5s | 0.30 |
| yokai-watch | 21 | 842 | 43.5% | 17.2s | 0.28 |
| 2ch-matome | 34 | 912 | 52.7% | 15.4s | **0.16** |
| clip-lab | 7 | **4,136** | 57.7% | 20.4s | **0.00** |
| fake-paper | 8 | 523 | 33.0% | 13.0s | 0.00 |

全体: 176本 / 171,058再生 / 61登録 / 0.357登録per千再生。

**記録しておくべき2点:**

1. **2ch-matome は閲覧指標が悪くないのに登録だけ壊滅的**（AVP 52.7%＝2位、平均再生912＝3位、
   なのに登録/千 0.16＝最下位で company-facts の 1/5）。「面白かった」で完結し継続視聴の
   動機が生まれていない。**尺や速度の問題ではなく企画構造の問題**であり、次回 company-facts型
   （継続的に得する情報）の要素を投入して比較する。未着手。
2. **clip-lab は平均再生4,136で断トツ首位のまま登録0**。09-02 メモの「止まっている clip-lab が
   1本あたり再生で断トツ1位」は事実だが、**登録には1人も繋がっていない**点を併記しておく。
   素材追加の優先度を上げる根拠としては、再生数だけを見ると過大評価になる。

## 7. 本日のコンフィグ変更（`data/channels/*.json`・バックアップは `*.bak_pdca_20260903b`）

`data/channels_orchestrator/*.json` は `data/channels/*.json` への symlink なので両者は自動同期。

1. `title_rules` に機械可読フラグを追加: `require_shoutai_per_batch=1` /
   `require_number_with_unit=true` / `require_question_mark=false`
2. `theme_priority.title_style` を「断定形＋具体数字＋単位」既定に書き換え（旧値は
   `title_style_prev_20260903` に退避）
3. `voice_style.style_rules` の先頭にあった 08-23 のタイトルルールを削除し、訂正版を挿入
4. `short_format.extra_rules` の先頭に高評価CTA優先ルールと4・5行目の数字必須ルールを挿入
5. テーマキュー **+28本**（pokemon-lab 3→15 ※枯渇寸前だった / daily-science 12→18 /
   scp-lab 10→16 / yokai-watch 15→19。2ch-matome 18・company-facts 20 は据え置き）
   補充テーマは全て「正体」または数字＋単位を含む形で作成
6. 各chの `pdca_log` に変更履歴と反証条件を追記

**★反証条件（09-08 期限）: 「正体」有無の登録/千再生を再測定し 1.2倍を下回れば必須化を撤回する。
同日に断定形と疑問形の比較も行う。50%地点の維持率が 0.45 を超えたかも確認する。**

## 8. 前夜から変わっていない（＝3日以上止まっている）もの

- OAuth 失効（→1）※悪化して全13chへ拡大
- `ANTHROPIC_API_KEY` 未設定 → clip-lab 海外枠が失敗し、依頼書が
  `data/analytics/viral_translation_pending/` に滞留（09-02 から件数変化なし）
- 切り抜き4ch の素材枯渇（clip-lab 国内 / clip-fukada / clip-animal）
- `socio-rx` の運用可否が未決
- BGM 全ch未設定
- CTA 遵守率の効果測定（09-01 から3日連続で未実施）
- 電話認証が daily-science 以外の12chで未完了
- `~/.auto-memory` が接続フォルダ外（5日連続）

## 9. 生成物

- `reports/youtube_analysis_20260903.xlsx`
  （サマリー / チャンネル別詳細 / 動画別パフォーマンス / 改善アクション / トレンド の5シート）
- `restart_and_trigger_20260903.command`（Mac 上で実行する。再認可 → トリガ）

## 10. 指揮者の実行環境についての注意（毎回ここで詰まる）

指揮者はサンドボックス Linux VM 上で動作しており、**Mac の `localhost:8000` へ到達できない**
（`curl` は接続不可）。したがって以下は指揮者からは実行できず、`.command` に落として
ユーザーに実行してもらう必要がある:

- Phase 1 の YouTube Data API 実績取得（OAuth トークンは Mac 側にある）
- Phase 4 の autopilot トリガ

なお **autopilot は Mac 上で既にスケジュール稼働しており**、本日も scp-lab が 08:15 に発火済み。
各chの発火時刻は company-facts 16:15 / 2ch-matome 17:15 / scp-lab・yokai-watch 18:15。
**通常は手動トリガ不要**で、再認可がこれらの時刻より後になった場合のみ手動発火すればよい。

また 09-02 の `.command` が使っていた `POST /api/autopilot/{ch}/trigger` は**存在しないパス**。
正しくは `POST /api/channels/{ch}/autopilot/run-now` で、`Authorization: Bearer` が必須
（`POST /api/auth/login` に `APP_PASSWORD` を投げてトークンを取る）。09-03 版では修正済み。

---

## 11. ★ 同日の検証で見つけた自分のミス（次回必ず読むこと）

サブエージェントによる独立検証で、7章の変更に**2件の実害ある退行**が見つかった。修正済みだが、
同じ間違いを繰り返さないために原因を残す。

### 退行1: `theme_priority.title_style` を6ch一律の文字列で上書きしてしまった

新しい共通ルールを書くつもりで、各チャンネル固有の指示ごと置き換えていた。消えていたのは:

- **2ch-matome**: 「掲示板に立ったスレタイそのもの。一人称は『ワイ』、語尾に『w』。
  勝ちパターン4型…『〜やけど質問ある？』」
- **scp-lab**: 「必ず具体的なSCP番号を出す」
- **company-facts**: 「企業名を必ずタイトルに入れる」

しかも新しい共通文には「断定形の言い切りを既定とし、疑問符は必須にしない」と書いてあり、
**2ch-matome の勝ちパターン（スレタイ型・疑問形）と正面から矛盾**していた。
`title_style` は `backend/pipeline/auto_scenario/generator.py:1110` と `:2805` で
実際に生成プロンプトへ渡るため、放置すれば台本の質に直接響いた。

→ 旧文を復元し、新知見は **`──【2026-09-03 …追記】──` として末尾に足す形**に改めた。
追記側にも「上の既存方針を打ち消さず、その上に重ねること」「上のチャンネル固有方針が疑問形を
要求する場合はそちらを優先する」と明記した。6chの `title_style` ハッシュが全て異なることを確認済み。

> **教訓: チャンネルコンフィグの自然文フィールドは上書きせず追記する。
> 6chに同じ文字列を書き込んでいる時点で、ほぼ確実に何かを消している。**

### 退行2: `title_rules` に足したフラグは backend が一切読まない死に設定だった

`require_shoutai_per_batch` / `require_number_with_unit` / `require_question_mark` を
「機械可読フラグ」として追加したが、`backend/` を全文検索しても `title_rules` を参照する
コードは**1件も無い**。`max_chars` も `forbid_patterns` も同様に未使用。

**generator.py が実際に読むのは以下だけ:**

| フィールド | 参照回数 |
|---|---:|
| `theme_priority`（`title_style` / `good_examples` / `viral_hooks`） | 11 |
| `short_format`（`structure` / `extra_rules`） | 9 |
| `voice_style.style_rules` | 3 |

→ フラグは記録として残しつつ `title_rules.enforced_by_backend = false` と
`enforcement_note` を全6chに明記した。実効ルールは `title_style` 側へ移した。

> **教訓: コンフィグを変える前に「backend がそのフィールドを読むか」を grep で確認する。
> 読まないフィールドに書いた施策は、適用済みに見えて何も起きていない。**

### 退行3・4: 補充漏れと掃除漏れ

- 6ch一律の方針変更なのに、テーマ補充が4chのみで **2ch-matome と company-facts が0本**だった。
  両chの現行キューに「正体」を含むタイトルも0本。
  → 各chの固有方針を守った形で6本ずつ補充（2ch-matome はスレタイ型「ワイ〜の正体」、
  company-facts は「企業名＋年収数字＋正体」）。最終キュー: daily-science 18 / scp-lab 16 /
  2ch-matome 24 / pokemon-lab 15 / yokai-watch 19 / company-facts 26。
- 既存キューに30字超が15本残っていた（最長44字）。→ 短縮し、原文は `title_original_20260903` に退避。
  全6chで30字超ゼロを確認済み。

### 検証で確認できたこと（数値は全て再現）

独立クエリで再計算した結果、「正体」1.530倍 / 「？」0.615倍 / 高評価率4分位
0.2316・0.3708・0.5184・0.7467 が**小数第2位まで一致**。チャンネル別内訳も一致した。
分析部分に誤りは見つかっていない。

ただし **pokemon-lab の「正体」は n=3** で採用基準ギリギリ。1.74倍を他3chと並列に扱うのは
やや誇張であり、09-08 の再測定では pokemon-lab を除いた3chでの効果量も併記すること。

### 検証で見つかった `.command` の軽微な不具合（未修正）

- 末尾で案内している `POST /api/analytics/fetch` は存在しないパス。実在するのは
  `/api/analytics/sync/{channel_id}` / `reach/ingest-all` / `analyze/{channel_id}`。
- 疎通確認の `/api/health` も存在しない（正しくは `/health`）。`curl` に `-f` が無いため
  404 でも成功扱いになるが、`/docs` へのフォールバックがあるのでサーバ停止は検出できる。
