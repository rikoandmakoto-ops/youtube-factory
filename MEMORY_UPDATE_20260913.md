# MEMORY UPDATE — 2026-09-13（指揮者 / 日次）

## 0. 結論（1行）

**09-09 以降、全チャンネルの OAuth が失効して公開が 5 日間ゼロ。生成は正常。滞留 95 本。
これが解けるまで他のすべての改善は効果が測れない。**

---

## 1. 障害の実体

| 項目 | 値 |
|---|---|
| 最終公開日 | **2026-09-08**（以降 09-09〜09-13 は 0 本） |
| backend.log のエラー | `invalid_grant` 1,005 件 / `Token has been expired or revoked` 996 件 / `RefreshError` 498 件 |
| 影響チャンネル | **全 13ch**（akashic / yokai / pokemon / fake-paper / daily-science / company-facts / scp-lab ほか） |
| トークン最終更新 | 全 ch とも 09-08〜09-09 に集中（`data/youtube_tokens.db` の `updated_at`） |
| 生成済み・未公開 | **96 本**（09-10: 44 / 09-11: 24 / 09-12: 21 / 09-13: 7）※生成は継続中なので増え続ける |

### 推定原因

各 ch は **別々の OAuth クライアント**を使っているのに、トークンがほぼ同時に失効している。
GCP の OAuth 同意画面が「テスト」のままだと、発行されたリフレッシュトークンは **7 日で失効**する。
この 7 日ルールが最有力。**同意画面を「本番」公開しない限り、再認可しても 7 日後に再発する。**

### 重要: パイプライン自体は健全

- APScheduler は 09-13 も daily-science / scp-lab / pokemon-lab / yokai-watch / company-facts /
  akashic を**自動発火し続けている**。手動トリガは不要。
- 生成・シナリオ・サムネ・MoviePy レンダリング・iCloud 同期まで全て成功している。
  `⚠️ 自動公開スキップ ... トークン失効のため要再認可` で**公開だけ**が落ちている。
- つまり壊れているのは 1 点だけ。そこを直せば滞留 95 本ごと復旧する。

### 逸失（推計）

- 停止前の公開ペース 17.2 本/日 × 5 日 ≒ 86 本
- 滞留 96 本 × コホート平均 806 再生 ≒ **77,300 再生**
- × 全ch 登録/千再生 0.491 ≒ **登録 38 人**

---

## 2. 本日の判断: 投稿設定を変更しなかった理由

コホート（公開 2026-08-10 以降 n=263）は **09-08 で完全に凍結**しており、09-12 の分析値と数値が一致する。

| ch | n | 平均再生 | 登録/千 | 高評価率 | 維持率 | 有効CTR |
|---|---|---|---|---|---|---|
| scp-lab | 52 | 648 | **0.771** | 0.409% | 38.6%（最下位） | 1.25% |
| company-facts | 40 | **1,092** | 0.709 | 0.334% | **56.1%** | **2.87%** |
| daily-science | 43 | 717 | 0.486 | **0.580%** | 48.2% | 1.97% |
| yokai-watch | 39 | 793 | 0.485 | 0.475% | 43.3% | 1.51% |
| pokemon-lab | 40 | 811 | 0.277 | 0.219% | 41.1% | 1.94% |
| 2ch-matome | 49 | 822 | **0.199** | 0.231% | 48.8% | 3.15% |
| **合計** | **263** | **806** | **0.491** | 0.367% | 45.8% | 1.98% |

**さらに欠測がある**: 09-07（18本）・09-08（14本）の計 **32 本は再生数が 0 で記録されている**。
これは実績ではなく、**analytics 同期も 09-08 前後で止まった**ことによる欠測
（`video_metrics.fetched_at` の最大値が 2026-09-08 14:19 UTC）。
つまりコホート 263 本のうち直近 32 本は**一度も計測されていない**。復旧後に backfill が必要。

同じデータで二度目の意思決定をすると効果検証ができなくなるため、
**投稿枠・voice_style・スタイルルールは一切触っていない**。再評価は復旧後 n≧20/ch が揃う
**2026-09-20 目安**。（C1〜C4（09-12 適用分）の検証も同じタイミング）

---

## 3. 新規知見: pokemon-lab の「どっちが勝つ？」型は登録に効かない

pokemon-lab / 公開 2026-07-20 以降 / views≥150 / **n=33**

| 型 | 本数 | 平均再生 | 登録/千再生 | 維持率 |
|---|---|---|---|---|
| **数値提示型** | 16 | 874 | **0.429** | 34.3% |
| その他 | 10 | 944 | 0.212 | 44.6% |
| 対決（どっちが）型 | 7 | **1,493** | **0.191** | 53.7% |

- 対決型は**再生を 1.7 倍稼ぐが、登録変換は数値提示型の 1/2.2**。維持率も最高（53.7%）なのに登録に繋がらない。
  → **維持率・再生数は登録を説明しない**という既存の所見と整合する。
- pokemon-lab は登録/千 0.277 で 6ch 中ワースト2。にもかかわらず**キュー先頭が対決型で占められていた**。
  これが当 ch の低迷の直接要因である可能性が高い。
- 実測トップも数値提示型: ハピナス HP255・特防（1.06）、ヌメルゴン特防（1.05）、シェイミ127（1.16）。

**対応**: 対決型は禁止しない（再生の絶対値は出るため）。キュー構成比を数値提示型 ≥70% に是正し、
対決型は末尾へ回した。検証は復旧後コホートで。

---

## 4. 各 ch の勝ち型（実測トップから抽出・補充テーマの型として採用）

| ch | 勝ち型 | 実測トップ例（登録/千） |
|---|---|---|
| **scp-lab** | SCP番号 + 具体的な数字/時間 + **消失・途絶**の未解決な問い | 1283-JP 踏切で消えた **3.03** / 096 記録消失 2.20 / 1987「24時間ごと」2.10 / 4335 観測途絶 2.08 |
| **daily-science** | 毎日起きる身体感覚 + 具体的な倍率/秒数 | 座ると腰だけ痛い「圧力1.4倍」**3.47** / 濡れた紙は「3倍」破れやすい 2.42 |
| **company-facts** | 企業名 + 具体年収/金額 + 「実は / 本当はどこに」 | Netflix 月額1,590円どこに消える **1.98** / サントリー年収1,000万・残業19.5h 1.89 |
| **pokemon-lab** | 種族値など具体数値の提示（対決型ではない） | ハピナス HP255・特防 1.06 / シェイミ2形態127 1.16 |

---

## 5. 本日のコンフィグ変更（Phase 3・適用済）

`scripts/orch_apply_20260913.py`（`data/channels/` と `data/channels_orchestrator/` の両方へ反映）

| ch | キュー | 残日数 | 内容 |
|---|---|---|---|
| scp-lab | 10 → **21** | 3.3d → 7.0d | 「番号+数字+消失/途絶」型で補充 |
| daily-science | 13 → **21** | 4.3d → 7.0d | 「身体感覚+倍率/秒数」型で補充 |
| pokemon-lab | 12 → **21** | 4.0d → 7.0d | 数値提示型で補充 **＋ 対決型を末尾へ再配置** |
| company-facts | 18 → **28** | 4.5d → 7.0d | 「企業名+年収+実は」型で補充 |
| 2ch-matome | 17（変更なし） | 5.7d | 在庫充足 |
| yokai-watch | 26（変更なし） | 8.7d | 在庫充足 |

バックアップ: `*.bak_pdca_20260913_orch` / 変更サマリ: `reports/orch_config_changes_20260913.json`

---

## 6. 見送った作業と理由

| 作業 | 理由 |
|---|---|
| moviepy 6ch への手動トリガ | APScheduler が自動発火済み。公開が通らない状態での追加生成は未公開在庫 95 本への積み増しにしかならない |
| 投稿枠 / voice_style / スタイル変更 | コホートが 09-08 で凍結。同一データでの二重意思決定になる |
| サムネ改善（daily-science） | 高評価率 1位・有効CTR 1.97%（中位）で伸びしろは確かにあるが、検証用の新規公開が出せない |

---

## 7. 仕様との差分（次回の指揮者タスクで注意）

- **制作トリガの正しいエンドポイントは `POST /api/channels/{channel_id}/autopilot/run-now`。**
  スキル本文の `POST /api/autopilot/{channel_id}/trigger` は**存在しない**（`backend/api_channel_autopilot.py:1075`）。
- `company-facts` の `autopilot.schedule` 直下 `days_of_week` が `[3,4,5]` で `times` の全曜日と食い違うが、
  `api_channel_autopilot.py:456` が `slot.get("days_of_week") or days` で**スロット側を優先**するため実害なし。修正不要。
- `video_metrics.ctr` は**百分率ではなく比率**（0.0121 = 1.21%）。集計時に 100 倍すること。
- **`data/channels_orchestrator/{ch}.json` は `../channels/{ch}.json` へのシンボリックリンク**
  （git 上も mode 120000）。本日 Phase 3 で一時的に実体ファイルへ置き換えてしまい、`ln -sfn` で復元した。
  **両ディレクトリへ個別に書き込んではいけない。`data/channels/` 側を書けば自動的に反映される。**
  `scripts/orch_apply_20260913.py` は `tgt.is_symlink()` で skip するよう修正済み。
  なお `data/channels_orchestrator/*.bak_pdca_20260913_orch`（6件）は**変更後の内容が入った無効なバックアップ**
  なので使わないこと。ロールバックは `data/channels/*.bak_pdca_20260913_orch` を使う（変更前の状態が正しく入っている）。

---

## 8. 未対応・要人手（前日から継続）

- `clip-animal`: 許諾済み外部素材が `max_duration_sec(3600s)` 超で 5 本除外（最長 4328s）。素材が枯渇。
- `clip-lab` / `clip-kaneko`: `ANTHROPIC_API_KEY` / `OPENAI_API_KEY` 未設定で viral エンジン停止。
  「壊れた字幕で公開しない」ガードが正しく働いて中止している。
- `akashic-librarian`: OAuth 未連携（生成成功・公開のみスキップ）。今回の全ch失効とは別件。
- `retention_curve` が主力 6ch 中 5ch で未取得。離脱点分析が daily-science でしかできない。

---

## 9. 本日の成果物

| ファイル | 内容 |
|---|---|
| `scripts/orch_recover_20260913.command` | **★最優先★** OAuth 復旧手順（診断 → 同意画面公開 → 再認可 → 検証） |
| `scripts/orch_apply_20260913.py` | Phase 3 コンフィグ更新（実行済み） |
| `scripts/orch_xlsx_20260913.py` | Phase 5 xlsx 生成 |
| `reports/youtube_analysis_20260913.xlsx` | 5シート（サマリー/チャンネル別詳細/動画別パフォーマンス/改善アクション/トレンド） |
| `reports/orch_config_changes_20260913.json` | コンフィグ変更サマリ |
| `MEMORY_UPDATE_20260913.md` | 本ファイル |

---

## 10. 明日の優先順

1. **GCP 同意画面を「本番」公開 → 全 ch 再認可**（`scripts/orch_recover_20260913.command`）。
   これが通るまで他は全て無意味。**再認可だけでは 7 日後に再発する。**
2. 滞留 95 本の公開消化を確認する（本数・エラーの有無）。
   あわせて **09-07 / 09-08 公開分 32 本の metrics backfill**（`POST /api/analytics/sync/{ch}`）を回す。
3. 復旧後、`oauth_tokens.updated_at` を毎日の指揮者タスクで監視項目に加える
   （今回 5 日間気付けなかったのは監視が無かったため）。
4. `retention_curve` が主力 5ch で取れていない原因の調査。
5. C1〜C4（09-12）と本日の pokemon-lab キュー是正の検証は **復旧後 n≧20/ch** で。目安 2026-09-20。
