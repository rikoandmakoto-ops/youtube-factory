# メモリ更新差分 — 2026-09-09 午前（youtube-orchestrator）

実行: 2026-09-09 10:13–11:40 JST

---

## 0. 本日の結論（4行）

1. 🚨🚨 **稼働中のバックエンドは、指揮者が書いたチャンネル設定を上書きして消す。**
   10:38 に反映した `yokai-watch.json` が **11:15:13 にバックエンドに丸ごと書き戻され、
   本日の変更（`banned_words` / `forbid_patterns` / `title_style` / `pdca_log`）が全て消えていた。**
   検証で気づいて再適用したが、**再起動するまで再発しうる。**（→ §1）
2. 🚨 **`scp-lab` の投稿枠が二重登録で、1日6本作っていた。** `times` が
   `[(17,0),(13,0),(19,0),(17,0),(13,0),(19,0)]` と完全重複。3枠へ修正。
   ただしジョブIDが枠の**添字**なので、**再起動しないと古い idx=3,4,5 が残る。**（→ §2）
3. **「秘密」は登録者を止める語だった。** 登録/千再生 0.120（n=18）vs 0.527（n=178）＝4.4倍差。
   出現する4ch全てで同方向。全6chで機械ゲート禁止。（→ §3）
4. **`2ch-matome` の「ワイ〜」定型は、直近50本中16本（32%）を占めて
   10,972再生を集めながら登録者0人。** 禁止した。（→ §4）

> ⛔ `~/.auto-memory/` は **12夜連続で接続フォルダ外**。接続済みは
> `Developer` / `youtube-factory` / `iCloud Documents` / `Downloads` の4つのみ。
> 本ファイルと `HANDOFF.md` をメモリ本体として扱う。

> ⛔ 本日の指揮者はネットワークに到達できなかった（サンドボックスから
> `googleapis.com` はタイムアウト、ホストの `localhost:8000` は 403）。
> **Phase 1（実績取得）と Phase 4（制作指示）は未実行。**
> 分析は 09-08 23時取得のスナップショットで行った。
> 人手が要る工程は `trigger_20260909.command` にまとめてある。

---

## 1. 🚨🚨 稼働中バックエンドが指揮者の設定変更を上書きする

### 事実

| 時刻 | 出来事 |
|---|---|
| 10:38:1x | 指揮者が全6chの `data/channels/*.json` を更新 |
| 11:15:13 | **`yokai-watch.json` がバックエンドに書き戻される** |
| 11:33 | 検証で発覚。`banned_words` が `None`、`pdca_log` の 09-09 行も消滅 |
| 11:36 | 再適用して復旧 |

同じ時間帯に `daily-science` 10:38 / `pokemon-lab` 10:38 / `company-facts` 10:38 /
`scp-lab` 11:00 / `2ch-matome` 11:30 が書かれているが、こちらは変更が残っていた。
**yokai-watch だけが消えた**ので、全チャンネル一律ではなく
「そのチャンネルの設定を**プロセス起動時に読んだ状態**で保持しているワーカーが、
何かの契機で自分の持っている内容を書き戻す」挙動と見られる。

### なぜ起きるか（推定）

`theme_queue` は `theme_seeds` から30分ごとに再生成される揮発キャッシュで、
その再生成のたびにチャンネル JSON 全体が書き戻される。
書き戻す側が持っているのは**プロセスが読み込んだ時点のツリー**なので、
その後にファイル側で行われた編集は差分ごと消える。

### ★ 次の指揮者への申し送り（最重要）

- **設定を変えたら、必ず変更後に読み直して残っているかを確認すること。**
  書いた直後の成功ログは何の保証にもならない。
- **設定変更のあとはバックエンド再起動を人に依頼すること。**
  再起動しない限り、①変更が消える ②消えなくても稼働プロセスは
  古い設定で動き続ける、のどちらかになる。
- 検証スクリプトは `scripts/orch_verify_20260909.py` に置いた。
  symlink・枠の重複・機械ゲートの実効性・誤爆を一度に見る。

---

## 2. 🚨 `scp-lab` の投稿枠が二重登録されていた（1日6本）

```
"times": [(17,0),(13,0),(19,0),(17,0),(13,0),(19,0)]   ← 同一時刻が2回ずつ
```

`backend/api_channel_autopilot.py` は枠ごとに
`sch.add_job(..., id=_job_id(channel_id, idx))` で登録する。**ID が枠の添字**なので、
同じ時刻でも添字が違えば別ジョブとして生き、**同一時刻に2回発火**していた。

対処:
- `times` を3枠へ重複解消。
- あわせて `17:00` を `09:00` へ移した（当ch内実測 19時 1.23(n=9) / 13時 0.83(n=6) /
  9時 0.69(n=12)。17時は n<2 で有効データが無かった）。

> ⚠️ **稼働中プロセスには idx=3,4,5 の古いジョブが残っている。**
> `replace_existing=True` は idx 0〜2 にしか効かない。
> **再起動するまで scp-lab は1日6本作り続ける。**

---

## 3. 「秘密」は登録者を止める語（全6chで禁止）

| 区分 | n | 再生 | 登録 | 登録/千再生 |
|---|---:|---:|---:|---:|
| 「秘密」あり | 18 | 16,609 | 2 | **0.120** |
| 「秘密」なし | 178 | 191,590 | 101 | **0.527** |

ch内対照も4ch全てで同方向:

| ch | あり | なし |
|---|---:|---:|
| pokemon-lab | 0.122 (n=9) | 0.332 (n=20) |
| yokai-watch | 0.149 (n=7) | 0.543 (n=21) |
| daily-science | 0.000 (n=1) | 0.497 (n=34) |
| 2ch-matome | 0.000 (n=1) | 0.209 (n=37) |

★ **時間バイアスは結論と逆向き**: 「秘密」入りは公開が 08-30 までで古く、
登録を積む時間は長かった側。それでも負けている。

対処: 全6chの `title_rules.hard_constraints.banned_words` に `"秘密"` を追加。

> ⚠️ **コード側に矛盾が残っている（未対応）。**
> `backend/pipeline/title_constraints.py` の `_REASON_WORDS` に **「秘密」が入っている**。
> 「なぜ」始まりを機械的に言い換えるとき、「秘密」があると「理由語あり」と判定して
> 答え提示語を補わない。本日の実測では最悪の語なので、この判定は逆効果。
> `banned_words` の停止が先に効くので実害は限定的だが、**外すべき**。

---

## 4. `2ch-matome` の「ワイ〜」定型は再生を集めて登録0

直近50本のうち **16本（32%）** がこのテンプレ。**合計 10,972 再生・登録者 0 人。**
評価母集団（公開09-05以前・views>=200）に限っても n=11・10,781再生・登録0。
同ch内の非該当は 0.282（n=27）。

実例（すべて登録0）:

```
🤣ワイ、落雷を3秒で見たんやが質問ある？w🍑        928再生
🤣ワイ、大谷の昼飯を3秒で当てるスレw🤣           958再生
ワイ、GTA6妄想歴6年やけど質問ある？w💦          1,090再生
ワイのキーボードの変な癖あげてけw               1,190再生
```

対処: `forbid_patterns` に2件追加。

```
(?<![ハスロ])ワイ(?![ドヤンルフパブザ])   # 「ハワイ」「ワイド」「ワイヤレス」等を避ける
質問ある
```

★ 最初は行頭限定 `^\s*ワイ` にしたが **11本中7本しか止まらなかった**ので位置不問へ拡張。
当chの全タイトル50本で**誤爆ゼロ**を確認済み。

---

## 5. 答え提示語は語ごとに2倍の差がある

09-08 の「答え提示語を入れる」という結論自体は再現した
（あり 0.606 n=123 vs なし 0.302 n=73）。ただし**語によって効きが倍違う**。

| 語 | n | 登録/千再生 |
|---|---:|---:|
| 実は | 18 | **0.767** |
| 実態 | 9 | 0.681 |
| なぜ | 72 | 0.568 |
| 正体 | 25 | 0.510 |
| 本当 | 17 | 0.462 |
| 理由 | 19 | **0.368** |

現状 `daily-science` / `yokai-watch` の `repair_with` 先頭は **「正体」（下から3番目）**。
最上位の「実は」は**語尾に置けない**ので `repair_with` には入れられない
（`title_constraints.py` が明記している通り、機械修復は語尾に足すだけ）。

対処: `theme_priority.title_style` の先頭にこのランキングを入れ、
LLM 側の第一候補として「実は」を指示した。

> ⚠️ 自然文の `title_style` は **LLM に無視されることが 09-04 に実測確定している。**
> 09-16 に「実は」の出現率が上がっていなければ、`repair_with` の並べ替えへ切り替えること。

---

## 6. `scp-lab` — 固有IDは効き、記録票フォーマットは死んでいる

| 区分 | n | 再生 | 登録 | 登録/千再生 |
|---|---:|---:|---:|---:|
| `SCP-数字ID` あり | 27 | 24,787 | 22 | **0.888** |
| `SCP-数字ID` なし | 9 | 6,675 | 3 | 0.449 |
| 「— …【ラベル】」型 | 6 | 5,418 | **0** | **0.000** |
| それ以外 | 30 | 26,044 | 25 | **0.960** |

対処: 「— …【ラベル】」型を `forbid_patterns` で禁止（`—\s*.*【`）。

> ⚠️ **`SCP-数字ID` の必須化はできなかった。** `hard_constraints.require_any_of` は
> 語彙照合のみで、正規表現の必須化を表現する枠が無い（`require_pattern` が無い）。
> 現状は `title_style` の自然文で指示しているが、自然文は無視される。
> **`title_constraints.py` に `require_pattern` を足す必要がある（コード変更・未実施）。**

---

## 7. 投稿枠の変更（すべて ch 内対照）

| ch | 変更 | 根拠 |
|---|---|---|
| scp-lab | 17:00 → 09:00（＋重複解消） | 19時 1.23(n=9) / 13時 0.83(n=6) / 9時 0.69(n=12)。17時は n<2 |
| 2ch-matome | 07:00 → 09:00 | 7時 0.00(n=2) が最下位、9時 0.29(n=5) と 12時 0.29(n=7) が最上位 |
| pokemon-lab | 19:00 → 15:00 | 19時は n<2。隣接の18時 0.21(n=10) が最下位。15時 0.33(n=3) / 17時 0.31(n=10) |

据え置き（09-08 に最適化済みで実測上も妥当）:
`daily-science` 7:30/12:30/17:00（17時 0.74・n=17）、
`company-facts` 8:15/19:00/17:00（19時 0.94 / 17時 0.86）、
`yokai-watch` 12:00/9:30/17:00（12時 1.06・n=7）。

---

## 8. 判断軸の再確認（5回目）

**高評価率 → 登録** は再現した。

| 高評価率4分位 | n | 登録/千再生 |
|---|---:|---:|
| Q1 (0.07%) | 49 | 0.249 |
| Q2 (0.23%) | 49 | 0.473 |
| Q3 (0.40%) | 49 | 0.308 |
| Q4 (0.79%) | 49 | **0.968** |

**維持率は今回も無関係**（50%以上 0.471 / 50%未満 0.512）。
ch単位でも符号が逆で、維持率1位の pokemon-lab（49.8%）が登録/千 0.278 で下から2番目、
維持率最下位の scp-lab（36.8%）が 0.795 で1位。
`optimization.primary_metric = subs_per_1000_views` の据え置きを追認。

---

## 9. その他の反映

- `pokemon-lab` の `theme_seeds` を 23 → 33 本へ補充。`theme_queue` が12本と6ch中最少だった。
  **queue ではなく seeds に入れること**（queue は30分ごとに seeds から再生成される揮発キャッシュ）。
  題材は上位実績の「固有名詞＋数値を1つ提示」型に寄せ、
  0転換が並んだ「どっちが勝つ？」対戦比較型は増やしていない。
- `daily-science` の `theme_priority.viral_hooks` から「99%が知らない」系を削除。
  **同じ設定ファイルが、実測で劣位（0.194 vs 0.510）の語をバイラルフックとして推奨していた。**
- `data/channels_orchestrator/*.json` は 09-08 から `channels/*.json` への **symlink**。
  書き込み先は1箇所でよく、二重書き込みは不要（検証スクリプトで symlink の生存も見ている）。

---

## 10. 最大の取りこぼし

**再生 1,200 以上で登録者0** が **24本・合計 39,213 再生**（母集団全体の 19%）。
本日の禁止ゲート（秘密／99%／ワイ・質問ある／記録票フォーマット）は、この層を狙って設計した。

---

## 11. 環境メモ — サンドボックスから外に出られない

- `googleapis.com` → タイムアウト（YouTube Data API は叩けない）
- ホストの `localhost:8000` → 到達不可、`host.docker.internal` → 403
- したがって **Phase 1（実績取得）と Phase 4（制作指示）は指揮者側では実行できない。**
- ただし **Phase 4 は APScheduler が枠ごとに自動発火する**ので、
  手動 trigger を叩くと計画（1日3本）に**上乗せ**になる。叩かないのが正しい。
- `reports/*.xlsx` の再計算に使う LibreOffice（`recalc.py`）は、初回は
  `success / 0 errors` で通ったが、その後ハングして復旧しなかった。
  代替として `scripts/orch_cache_values.py` を書いた
  （数式を残したままキャッシュ値を XML へ注入する）。
  **中断した LibreOffice が空の `<v/>` を残すので、「要素の有無」ではなく
  「中身が空でないか」で判定すること**（一度これで空のまま出荷しかけた）。

---

## 12. 生成物

| ファイル | 内容 |
|---|---|
| `reports/youtube_analysis_20260909.xlsx` | 5シート（サマリー / チャンネル別詳細 / 動画別パフォーマンス / 改善アクション / トレンド） |
| `trigger_20260909.command` | Phase 1 と再起動手順（Phase 4 は叩かない理由つき） |
| `scripts/orch_apply_20260909.py` | コンフィグ反映（冪等） |
| `scripts/orch_verify_20260909.py` | 反映結果の検証（37/37 停止・誤爆0） |
| `scripts/orch_xlsx_20260909.py` | xlsx 生成 |
| `scripts/orch_cache_values.py` | 数式キャッシュ値の注入（recalc の代替） |
| `scripts/orch_xlsx_verify_20260909.py` | xlsx を元データと突き合わせて検証 |

---

## 13. 明日の指揮者がまず見るべきもの

1. **バックエンドが再起動されたか。** されていなければ §1・§2 は未解決のまま。
2. **09-09 の設定変更が残っているか。** `scripts/orch_verify_20260909.py` を実行する。
3. 「秘密」「ワイ」の禁止後、`2ch-matome` の総再生がどれだけ落ちたか。
   落ち幅が3割を超え、かつ登録/千が 0.28 を超えないなら撤回を検討（xlsx の反証条件に記載）。
4. 「実は」の出現率が上がったか。上がっていなければ `repair_with` の並べ替えへ。
