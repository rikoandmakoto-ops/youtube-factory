# メモリ更新差分 — 2026-09-08（youtube-orchestrator 定時実行）

実行: 2026-09-08 10:03–10:20 JST
出力: `reports/youtube_analysis_20260908.xlsx`（5シート・602数式・エラー0）
制作指示: `restart_and_trigger_20260908.command`（Mac上でダブルクリック実行が必要）

---

## 0. 本日の結論（3行）

1. **維持率は登録者をまったく説明しない。** 維持率50%以上/未満で 登録/千再生 は
   **0.47 vs 0.47** と完全に無差別だった。説明するのは高評価率だけで、
   4分位の最上位は最下位の **3.9倍**（0.85 vs 0.22）。09-04 に決めた
   `decision_metrics = ["subs_per_1000_views"]` の単独採用は本日も正しい。
2. **効くのは「答えを提示するタイトル」。** 答え提示語（理由/正体/本当の/実は/わけ/
   なぜ/真相/裏側/実態）ありは 登録/千再生 **0.57**、なしは **0.31**（1.84倍・n=196）。
   ところが維持率は逆に **45.0% vs 58.0%** で提示型のほうが**低い**。
   **「最後まで見せる」と「登録させる」は別の指標である。**
3. 🚨 **2ch-matome は再生1位(39,522)なのに登録/千再生 0.20 で6ch最下位。**
   原因は面白さではなく**参加型フォーマット（答えを渡さない構造）**で、
   当ch内で 参加型 **0.06** vs 結論型 **0.32**（5.8倍差）。
   **本日フォーマットごと撤回した（本日最大の変更）。**

---

## 1. ★★ 本日の中核 — 維持率は捨ててよい、高評価率だけを見ろ

09-07スナップショット（moviepy系6ch・286本、うち200再生以上 n=196）。

| 切り口 | 群A | 群B | A 登録/千 | B 登録/千 | 倍率 |
|---|---|---|---:|---:|---:|
| **高評価率4分位** | Q4 (0.52–1.45%) | Q1 (0.00–0.17%) | **0.85** | **0.22** | **3.9倍** |
| **維持率しきい値** | avp≥50% | avp<50% | **0.47** | **0.47** | **1.00倍（無差別）** |
| **タイトル答え提示語** | あり | なし | **0.57** | **0.31** | **1.84倍** |
| 公開からの日数 | 4–7日 | 15–30日 | 1.09 | 0.34 | 3.2倍 |

（本表は独立に再計算して検証済。1〜4・6〜8 の全項目が一致した。）

> ★ **ch内対照でも維持率は逆行する。** 高維持側が劣後したのは
> scp-lab 0.54 vs 0.81 / pokemon-lab 0.20 vs 0.33 / 2ch-matome 0.18 vs 0.22 /
> company-facts 0.68 vs 0.73 の **4/6ch**。優位だったのは daily-science と yokai-watch のみ。
> **「維持率を上げれば登録が増える」は本データでは成立しない。**
>
> ★ **高評価率と維持率は無関係どころか逆。** 高評価率Q1の平均維持率は 56.2%、
> Q4は 44.5%。**維持率＝スクロールされなかった（受動）／高評価＝能動的な承認**で、
> 登録も能動的な行動だから高評価と揃う、という説明が最も整合する。
>
> ★ **再現4回目。** 08-29 → 09-03(n=339) → 09-04 → 09-08(n=196) で毎回同方向。
> この指標だけは信頼してよい。

---

## 2. 🚨 2ch-matome — 「再生は取れるが登録に変換しない」の正体は参加型フォーマット

判定は `views>=200` の38本。参加型 = タイトルに あげてけ／質問ある／当てるスレ／晒してけ を含むもの。

| 型 | n | 登録/千再生 | 高評価率 | 平均再生 | 平均維持率 |
|---|---:|---:|---:|---:|---:|
| 参加型 | 17 | **0.06** | 0.235% | 1,053 | **58.8%** |
| 結論型 | 21 | **0.32** | 0.241% | 1,029 | **41.1%** |

> ★ **平均再生はほぼ同じ（1,053 vs 1,029・1.02倍）。差は登録転換にだけ 5.8倍 出る。**
> しかも**維持率は参加型のほうが 17.7pt も高い**（58.8% vs 41.1%）。
> つまり**再生数でも維持率でも参加型が勝って見える。この2つを判断軸にしていたら
> この問題は永久に見つからないどころか、参加型を「勝ちパターン」として強化していた。**
> 09-04 に判断軸を subs_per_1000_views 単独にした判断が、本日ここで効いた。
>
> ⚠️ **群分けの正規表現に単独の `w` を入れてはいけない。** 結論型にも語尾 `w` は付く
> （例:「なぜPS5 Proに期待する？値段で胃が痛い理由とはw」）ので、`w` を含めると
> 群が逆転する。判定語は上記4語のみに限ること。

当chの `title_style` は「勝ちパターンは4型: (1)〜あげてけ (2)〜当てるスレw
(3)〜やけど質問ある？ (4)なんか〜なんやが」と宣言していたが、**(1)(2)(3) はすべて参加型**で、
`short_format` も「参加型のお題は正解を断定せず、視聴者が『いや、それより〇〇やろ』と
コメントしたくなる余白を残す」と明記していた。**この設計そのものが登録ゼロの原因。**

実測の勝ち本（結論型）:
```
2登録 /  914再生  実はマックに神メニュー3品が決まらない本当の理由とは？
2登録 / 1316再生  なんかPCの画面チカチカするんやが
1登録 / 1039再生  動画作りに7万課金した男、なぜ昼飯が塩むすびになったのか？
```

---

## 3. チャンネル別ランキング（登録/千再生・09-07スナップショット）

| 順 | ch | 本数 | 累計再生 | 累計登録 | 登録/千 | 高評価率 | 平均維持率 |
|---:|---|---:|---:|---:|---:|---:|---:|
| 1 | scp-lab | 50 | 32,165 | 24 | **0.75** | 0.392% | 32.8%（最下位） |
| 2 | company-facts | 39 | 43,330 | 30 | **0.69** | 0.369% | 46.1% |
| 3 | daily-science | 50 | 31,949 | 18 | 0.56 | 0.526%（最高） | 49.0% |
| 4 | yokai-watch | 50 | 32,997 | 12 | 0.36 | 0.442% | 44.1% |
| 5 | pokemon-lab | 50 | 33,440 | 9 | 0.27 | 0.281% | 42.3% |
| 6 | 2ch-matome | 47 | 39,522 | 8 | **0.20** | 0.238%（最下位） | 42.0% |
| — | **合計** | **286** | **213,403** | **101** | **0.473** | 0.375% | 42.7% |

> ★ **scp-lab が維持率6ch最下位(32.8%)なのに登録1位(0.75)。** 逆行の最も明確な事例。
> ★ **登録0本の割合**: 2ch-matome 41/47 / pokemon-lab 42/50 / yokai-watch 42/50 が登録ゼロ。

---

## 4. 投稿時刻（JST）— 17時が最良、18時と15時は捨てる

| 時 | n | 登録/千 | 高評価率 | 判定 |
|---:|---:|---:|---:|---|
| **17** | 39 | **0.70** | 0.369% | 最良帯 |
| 12 | 24 | 0.59 | 0.425% | 良 |
| 13 | 10 | 0.56 | 0.479% | 良 |
| 19 | 29 | 0.49 | 0.410% | 中位 |
| 9 | 23 | 0.45 | 0.281% | 中位 |
| 14 | 10 | 0.26 | 0.374% | 要移設 |
| **18** | 36 | **0.24** | 0.337% | 要移設 |
| **15** | 6 | **0.00** | 0.356% | 要移設 |

**移設した3枠（すべてch内対照に基づく）:**

| ch | 変更 | ch内根拠 |
|---|---|---|
| company-facts | 13:30 → **19:00** | 19時 0.94(n=4) / 17時 0.85(n=10) vs 14時台 0.38(n=6) |
| pokemon-lab | 17:30 → **17:00** | 17時台 0.41(n=10) vs 18時台 0.21(n=10) |
| yokai-watch | 17:45 → **17:00** | 19時台 0.24(n=14) が当ch最下位 |

> ⚠️ **17:30 / 17:45 の枠は publish_lead_minutes により公開が18時台へ落ちていた。**
> 枠の設定時刻と実際の公開時刻がずれている。今後の枠設計は `published_at` 実測で確認すること。
> ⚠️ **2ch-matome は時刻を変えていない。** 全時間帯が 0.00〜0.29 で一様に悪く、
> 問題が時刻ではなくフォーマットにあることがデータから明らかだったため。

---

## 5. 本日のコンフィグ変更（`data/channels_orchestrator/` と `data/channels/` の両系に反映済）

バックアップ接尾辞: `.bak_pdca_20260908_orch`

1. **【全6ch】** `title_rules.require_answer_marker = True` ＋ `answer_markers` 9語を追加。
   `title_style` と `short_format.extra_rules` にも実測根拠つきで明記。
2. **【2ch-matome】** `short_format` 3〜5行目「正解を断定せず余白を残す」を**撤回** →
   5行目で必ず「つまり〇〇やったんや」と答えを断定して終える構成へ。
3. **【2ch-matome】** `short_format` 6行目CTAを**高評価＋登録に一本化**（コメント誘導を削除。
   コメント誘導は `publish_settings.auto_comment` 側で継続）。
4. **【2ch-matome】** `theme_queue` 参加型27件を削除し結論型20件を投入（40→33件）。
   さらに答え提示語の無い「なんか〜なんやが」型7件を書き換え、**33/33件が答え提示型**に。
   `title_style` の勝ちパターン4型のうち参加型3型を撤回。
5. **【3ch】** 投稿枠の移設（§4）。**★ APScheduler 再登録のため backend 再起動が必須。**
6. **【yokai-watch】** `theme_seeds` の33字違反シード1件を24字へ短縮
   （「件（くだん）の正体、予言して死ぬ牛が生まれた理由」）。
7. **【全6ch】** `optimization.revalidated_20260908` に本日の実測を追記。

**反証期限 2026-09-15:**
- (a) 答え提示型100%化後も全体の 登録/千再生 が **0.47** を上回らなければ撤回
- (b) 2ch-matome の 登録/千再生 が **0.20 → 0.35** を超えなければ参加型撤回を戻す
- (c) 移設した3枠が移設前の実績を下回れば戻す

---

## 6. 検証

`backend/tests`（moviepy/fastapi 依存の2ファイルを除外）:
**変更前 8 failed / 477 passed → 変更後 7 failed / 478 passed。**

- ✅ **解消**: `test_title_gates_20260904.py::test_enforced_channels_have_clean_queues_and_seeds`
  （yokai-watch の33字シード違反。**変更前から失敗していた既存バグ**を本日直した）
- ⚪ **残る7件はすべて本日と無関係**: `ModuleNotFoundError: fastapi`（sandbox 環境起因）が6件、
  clip-animal / clip-kaneko のハッシュタグ数（本日の対象外ch）が1件。
- ✅ JSON妥当性・両系（orchestrator / channels）の同期・burst guard 90分・
  2ch参加型の残存ゼロ をすべて機械チェック済。
- ✅ xlsx は `recalc.py` で 602数式・エラー0。合計値を DB へ独立検算して一致
  （再生 213,403 / 登録 101 / 登録/千 0.4733）。

---

## 7. ⛔ 指揮者では解決できない積み残し（ユーザー操作が必要）

> **サンドボックスVMから Mac の localhost:8000 に到達できない**（`curl` code=000、
> googleapis も到達不可）。そのため **Phase1 の新規APIフェッチと Phase4 の制作指示APIを
> 直接実行していない**。分析は backend が **09-07 23:13 JST** に `analytics.db` へ
> 格納済みのスナップショットに基づく。制作指示は
> **`restart_and_trigger_20260908.command`（Mac上でダブルクリック）** に切り出した。
> このスクリプトは ①backend再起動（枠変更の反映に必須）→ ②ヘルスチェック →
> ③枠の反映確認 → ④6chへ trigger、の順で動く。

1. 🚨 **OAuth 7日失効**（09-07時点で clip-animal / socio-rx / fake-paper / akashic-librarian
   の4chが失効）。恒久対策は GCP同意画面を「テスト中」→「本番」へ公開してから13ch再認可。
   https://console.cloud.google.com/auth/audience （project 844705815004）
   **★順序厳守: ①本番公開 → ②再認可。逆だと7日後に同じことが起きる。**
2. 🚨 **サムネイル403**（09-07は 21/27本＝78%が403）。各chで
   https://www.youtube.com/verify をユーザー本人が実施する必要がある。
   `gen_thumbs_v4_all.py` の生成物は依然1枚も反映されていない。
3. 🚨 **`backend/.env` に `ANTHROPIC_API_KEY` が無い**。clip-lab の20:45枠が毎日落ちる真因かつ
   `series_engine` が全chで未応答になる原因。**環境変数1行で複数の症状が同時に消える。**
4. ⛔ **`~/.auto-memory/` が11夜連続で接続フォルダ外。** 接続済みは Developer /
   youtube-factory / iCloud Documents / Downloads の4つのみ。
   **恒久対応: Cowork の接続フォルダに `~/.auto-memory` を追加する。**
   当面は本ファイルと `youtube-factory/HANDOFF.md` をメモリ本体として扱う。

---

## 8. 明日（09-09）の指揮者が最初に見るべきもの

1. **`restart_and_trigger_20260908.command` が実行されたか。** 未実行なら枠移設は
   APScheduler に反映されておらず、§4 の3件は「未検証」であって「失敗」ではない。
2. **09-08 公開分のタイトルに答え提示語が入っているか。** 入っていなければ
   `title_rules.require_answer_marker` が backend から読まれていない可能性がある
   （09-03 に `title_rules` が死に設定だった前例あり。`enforcement_note` を確認すること）。
3. **2ch-matome の台本5行目が答えを断定して終わっているか。** ここが変わっていなければ
   本日最大の変更は1本も動画に出ていない。
4. **company-facts の3枠が403を免れるか。** 09-07 に07:34の1本だけ403を免れる不整合が出た。
   再現するなら「電話認証済み2ch」という理解自体を疑うこと。
