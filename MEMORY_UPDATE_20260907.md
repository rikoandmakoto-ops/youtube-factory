# MEMORY UPDATE — 2026-09-07（指揮者・朝）

集計窓: 2026-08-31〜09-06 / 対象: moviepy系6ch / 出典: `data/analytics/analytics.db`
分析シート: `reports/youtube_analysis_20260907.xlsx`
実行スクリプト: `restart_and_trigger_20260907.command`（Mac上で実行が必要）
ロールバック: `data/channels/<ch>.json.bak_pdca_20260907_orch`

---

## 1. 今日の最重要事項 — 09-03 のタイトル規約は誤りだった

09-03 に「全タイトルに数字＋単位を必須（1.56倍）」として全chへ入れたルールを、**本日撤回した。**

チャンネル内対照（公開08-10以降 n=227）で数字あり/なしを比較すると、対照可能な3ch全てで
**数字ありが劣後**していた。

| ch | 再生（数字あり/なし） | 登録転換（同） |
|---|---|---|
| 2ch-matome | 0.60倍 | 0.52倍 |
| pokemon-lab | 0.74倍 | 0.43倍 |
| yokai-watch | 0.58倍 | 0.35倍 |
| **中央値** | **0.60倍** | **0.43倍** |

09-03 に 1.56倍 と見えた原因は、daily-science 36/37・scp-lab 44/46・company-facts 34/34 と
**3chが既に数字入り比率97〜100%に偏っていた交絡**。09-03 自身が「チャンネル内対照で再検証」と
書いていたが、この3chには対照群が存在しないため実際には検証できていなかった。

**教訓（次回以降も必ず適用）**: ch内対照を名乗る前に、各chに「あり」「なし」が両方3件以上
存在するかを先に確認する。片側が0〜2件のchは判定から除外し、除外した事実を明記する。

### とった対応
`title_rules.require_number_with_unit` は **backend が読まないフィールド**（09-03 の検証で判明済み）
なので、実際に効く `title_rules.hard_constraints` へ移した。

| ch | hard_constraints | 理由 |
|---|---|---|
| 2ch-matome | `forbid_digits: true` | 数字が内容でない |
| yokai-watch | `forbid_digits: true` + 末尾疑問符禁止 | 同上。疑問形の登録が0.07倍と当chのみ壊滅的 |
| daily-science / scp-lab / pokemon-lab / company-facts | `max_digit_groups: 1` | 種族値・年収・SCP番号が内容そのもの。全面禁止は内容を壊す |

pokemon-lab は一度 `forbid_digits` にしたが、種族値がコンテンツの核なので `max_digit_groups: 1`
へ戻した。全面禁止は「数字が内容でない2ch」に限る。

---

## 2. 最大離脱は再生13〜22%地点。原因は構造にあった

`retention_curve` をセリフ単位で突合したところ、離脱の最大点が全ch共通で **再生13〜22%地点**に
集中していた（バケット別ドロップ early 1.77〜2.69% で intro の2〜3倍）。

落ちている行の正体は2つだけ:

1. **情報量ゼロの相槌行** —「へぇ〜、知らなかった！」「えっ、それ昨日やった！」
2. **専門用語・前提説明の初出** —「中枢パターン発生器」(-7.9%)、「でも電気タイプって相性だよね？」(-6.6%)

そして6行構成の **2行目がちょうどこの区間に当たる**。全6chの `short_format.structure` は
2行目を「リスナーのリアクション。**解説は始めない**」と規定していた。
つまり**視聴者が離脱を決める地点に、情報ゼロの行を構造として置いていた**。

09-03 に「4行目と5行目に数字を置く」という対策を入れたが、離脱地点そのものである2行目には
手を付けていなかったため効いていなかった。

### とった対応
全6chの2行目を「**リアクション＋新情報（数字か固有名詞）を同じ行に**」へ変更。
専門用語の初出は3行目以降に限定。ch別に文面を書き分けてある（2ch-matome は掲示板ノリを
壊さないよう「具体を1つ足す」程度に留めた）。

---

## 3. 投稿時間 — 午後半ばが全時間帯で最下位

JST時間帯別（公開08-10以降 n=227）:

| JST | 本数 | 平均再生 | 登録/千再生 |
|---|---|---|---|
| 9時 | 21 | 962 | 5.50 |
| 17時 | 50 | 905 | 4.48 |
| 19時 | 38 | 821 | 3.56 |
| 12時 | 28 | 633 | 2.82 |
| **15時** | 7 | **590** | **0.00** |
| **16時** | 4 | **226** | **1.11** |

pokemon-lab 15時→19時、yokai-watch 16時→9時30分へ移設。
**同一ch内の枠は90分以上あける制約**があり（`tests/test_title_gates_20260904.py` の連投ガード）、
最初 pokemon-lab を9時にして 8:30 と30分差になりテストが落ちた。時間を動かすときは必ずこの
制約を先に確認すること。

**反映には backend 再起動が必要**（APScheduler が起動時にジョブを登録するため）。

---

## 4. backend のバグを2件修正 — `pipeline/title_constraints.py`

1. **桁区切りコンマで数字が2個に数えられていた**
   `_DIGIT_GROUP_RE = r"[0-9０-９]+"` のため「1,590円」「0.3秒」「SCP-2006」が数字2個と判定され、
   `max_digit_groups: 1` を入れた瞬間に company-facts の正常なタイトルが軒並み違反になる状態だった。
   区切り文字（`,．-/：`）を跨いだ連なりを1グループとして数えるよう修正。

2. **数字除去で日本語が壊れていた**
   助数詞リストに「名」が無く「12名」→「名」が残る、序数の「〜目」も残る（「3回目」→「目」）。
   さらに「元ネタは3つある」→「元ネタはある」のように助詞が衝突する。
   助数詞を追加したうえで、**壊れた日本語になる場合は機械修復を諦めて原文を返すガード**を追加した
   （規約違反のタイトルより壊れた日本語のほうが害が大きい、という判断）。

`tests/test_title_gates_20260904.py` 14件、`test_short_format.py` 17件、全て通過。

**教訓**: 機械修復（repair）はテーマキューに一括適用してはいけない。今日一度やって
「毎朝5分だけ寝坊」→「毎朝だけ寝坊」のように壊したので、バックアップから復元して
違反14件を手作業で書き直した。

---

## 5. チャンネル序列（登録転換率＝至上目標に直結）

| ch | 再生(7日) | 登録 | 登録/千再生 | 維持率 | 実測CTR |
|---|---|---|---|---|---|
| company-facts | 191,430 | 143 | **0.747** | 56.9% | 2.28% |
| scp-lab | 168,878 | 113 | 0.669 | **44.2%↓** | 1.15% |
| daily-science | 164,809 | 96 | 0.582 | **59.6%↑** | 1.56% |
| pokemon-lab | 175,408 | 49 | 0.279 | 52.6% | **0.43%** |
| yokai-watch | 170,267 | 43 | 0.253 | 52.0% | 1.72% |
| 2ch-matome | 175,946 | 31 | **0.176** | 51.4% | 0.86% |

- **2ch-matome が構造的課題**: 再生は2位なのに登録は最下位で、首位の4.2分の1。
  再生を登録に変換できていない。CTA文言の調整では解けていない。**次回の主題とする。**
- **scp-lab の維持率が下降中**: 08-30 45.7% → 09-06 42.1%。6ch中最低。
- **pokemon-lab の CTR 0.43%**: 6ch最低。サムネ改善の最優先対象だが、今日はタイトルと台本構造を
  変えたため、効果の切り分けを優先して同時変更を避けた。
- **daily-science の131%は幻**: 08-28以前の維持率131%はループ計上。08-30に実測へ切替わり58%台。
  実勢は上昇中（58.9→61.2）なので、8月との単純比較をしないこと。

---

## 6. 判定を保留した件

- **1日3本体制（09-04開始）の是非**: 公開3日後の平均再生で見ると 09-03コホート 907回は8月下旬
  （700〜900回）と同水準。ただし増量後の09-04〜06は YouTube Analytics 未反映で判定不能。
  総再生数は09-03以降28万回台で横ばい＝本数を倍にしても総視聴が増えていない可能性がある。
  **09-08以降に09-04〜06コホートの3日後実績で再判定。8月水準を割っていれば1日2本へ戻す。**

---

## 7. 引き継ぎメモ

- `data/channels_orchestrator/*.json` は **`data/channels/*.json` へのシンボリックリンク**。
  片方を書けば両方に反映される。二重に書く必要はない。
- autopilot の制作指示エンドポイントは `POST /api/channels/{id}/autopilot/run-now`。
  SKILL.md に書かれている `/api/autopilot/{id}/trigger` は古い。
- 指揮者のサンドボックスから Mac の localhost:8000 へは到達できない。Phase 4 は
  `.command` ファイルを書いてユーザーに実行してもらう（従来どおり）。
- **要調査**: `data/youtube_tokens.db` の `expires_at` が `updated_at` より一律 28,801秒（8時間）前に
  なっている（全13ch）。公開自体は09-06に26本成功しており実害は出ていないが、失効判定を
  誤らせる可能性がある。
- **次回のコード改修候補**: `hard_constraints` に `min_chars` を追加する。タイトル実質20字下限は
  最も強いシグナル（再生1.53倍・維持率41.6% vs 27.1%・ch内対照の登録中央値2.67倍）だが、
  自然文の指示は LLM に無視される実績があるため機械強制が要る。
