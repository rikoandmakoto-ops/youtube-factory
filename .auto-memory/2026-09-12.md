# 2026-09-12（土）指揮者タスク

## 一行で

**新規実績は5日連続ゼロ。だから施策は変えず、「config とキューが内部で矛盾していた」2件だけを直した。**
最大の発見は **キュー補充が `title_constraints` を一度も通っていなかった**こと。

---

## 1. データ状況（実測）

| 項目 | 値 |
|---|---|
| analytics 最終スナップショット | **2026-09-08**（09-09/10/11/12 は `video_metrics` 0行 = **5日連続**） |
| `channel_metrics` | 09-05 で停止 |
| 公開実績 | **09-10 の2本が最後。09-11・09-12 は0本**（09-04〜08 は日25〜33本） |
| OAuth | **全13ch で `expires_at` が過去**。最新でも約52時間前。`updated_at` は全件 `expires_at + 8h` のまま＝**リフレッシュ成功歴ゼロ**（09-11 と同じ） |
| ANTHROPIC_API_KEY | `backend/.env` 18行目でコメントアウトのまま（**13夜連続**） |
| viral_translation_pending | 16件滞留（08-31〜） |
| backend.log | 79MB（09-11 は81.4MB） |
| pytest | 作業前 **14 failed / 4 errors / 603 passed** → 作業後 14 failed / 4 errors / **630 passed** |

- **ブラウザ収集は17夜連続で不成立。** 内蔵ブラウザで `youtube.com` のサイト承認を求めたが、無人実行のため**明示的に却下**された（`request_access` が declined を返す）。
  → **もう毎晩試すのをやめること。** タスク定義を `analytics.db` 基準に書き換えるか、ユーザーに事前承認を付与してもらうかの二択。本日は1回だけ試して即フォールバックした（所要1コール）。

### 30日窓（08-10〜09-08）の実力

合計 487本 / 4,563,673再生 / 1,931登録 / **登録per千 0.423**

| ch | 登録/千 | 再生 |
|---|---|---|
| scp-lab | **0.851** | 807,704 |
| company-facts | **0.733** | 279,533 |
| akashic-librarian | **0.727** | 23,383 |
| daily-science | 0.423 | 884,152 |
| pokemon-lab | 0.336 | 810,327 |
| yokai-watch | 0.324 | 743,631 |
| clip-fukada | 0.193 | 129,722 |
| 2ch-matome | 0.175 | 497,948 |
| clip-kaneko | 0.178 | 123,422 |
| clip-lab | **0.004** | 223,866 |
| fake-paper | **0.000** | 39,985 |

- **系統別: ゆっくり8ch 0.461 / 切り抜き3ch 0.101 → 4.58倍**（30日窓）。
  ⚠️ 09-08 のメモにある「11.5倍」は**同じ30日窓でも ch の数え方が違う**（当時はゆっくり6ch・切り抜き3chで集計）。倍率を引用するときは**窓と ch 構成の両方**を書くこと。
- **fake-paper は30日で登録0**（17本・39,985再生）。全11ch で唯一の登録ゼロ。
- **clip-lab は再生223,866に対し登録1**。相変わらず「再生は取れるが登録に一切つながらない」。

---

## 2. 本日の最大の発見

### 🔴 キュー補充が `title_constraints` を一度も通っていなかった

`theme_queue.replenish()` は LLM の提案をそのままキューに積んでいて、**各chが自分で設定したゲートを一度も参照していなかった**。結果：

- **未公開99件のうちゲート合格は24件（24.2%）**
- 最悪は **company-facts で12件全部が不合格**。理由は**自ch の `banned_words` に入っている絵文字（💸📉💰）を先頭に付けていた**から。**100%の自滅**。
- yokai-watch は `forbid_digits: true` なのに**10件中10件が数字入り**。config と生成側が真逆を向いている。
- scp-lab のキュー平均実効長 **39.2字**（自ch の `max_chars=30`）／ daily-science **35.8字**。

**対処**: `_annotate_title_gate()` を追加し、**弾かずに `title_gate_ok` の印を付ける**。
→ 弾く実装にしないのは、09-11 に「キュー全件ブロック」を起こしたため。印なら台本生成側が作り直せて、指揮者が毎日「補充の何割が落ちているか」を観測できる。

### 🔴 `title_constraints.repair()` をキューに当ててはいけない（本日実測）

57件に当てたら**ほぼ全てで日本語が壊れた**。repair は
- `max_chars` 違反 → **末尾切り落とし**
- `forbid_digits` 違反 → **数字の削除**
で通そうとするため：

| 元 | repair 後 |
|---|---|
| …愛らしい外見が起こした財団史上最恐の収容違反 | …愛らしい外見が**起こしの真相**（文法破壊） |
| …が[DATA EXPUNGED]した恐ろしい記録 | …が**[DATA EXP**（トークン途中切り） |
| シェイミの2形態を分ける気温**5度**の正体 | シェイミの2形態を分ける**気温正体**（事実欠落＋文法破壊） |
| ぬらりひょんの正体、江戸期**4図**で姿が変わった | …**江戸期図**で…（事実欠落） |
| 海底に沈んだ都市の入口 — …石造構造物は何を語るのか | …**何を語の正体**（文法破壊） |

**違反数は確かに減る。だから「合格」として通ってしまう。検査より悪い。**
→ **長さ・数字の違反は LLM 再生成に回す。** 機械修復してよいのは**絵文字除去だけ**（意味を一切変えない）。
→ この判断を `test_fixes_20260912_orch.py::TestRepairIsNotUsedOnQueueTitles` で固定した。repair の挙動が変わったらテストが落ちるので、そのとき**測り直す**こと。

※ モジュールの docstring は `min_effective_chars` について「機械修復はしない」と明記していたが、**`max_chars` と `forbid_digits` の repair は元から有効**だった。「repair は安全」と一般化しないこと。

---

## 3. 本日 config に加えた変更（実績由来ゼロ）

鉄則「**新規の再生実績が無い日は config を変更しない**」に従い、**実績に基づく施策変更は一件もしていない**。直したのは内部矛盾だけ。

1. **clip-lab / clip-fukada / clip-kaneko に `title_rules.hard_constraints` を新規付与**
   - この3chは `is_enforced()` が **False** で**検査が丸ごとスキップ**されていた（09-11 のメモの「未設定5ch」のうち稼働中の3ch）。
   - 入れたのは**禁止系のみ**: `秘密` / `99%が知らない` 型 / 連番プレフィックス / 絵文字 / `max_chars=48`。
   - **`min_effective_chars` と `require_any_of` は入れない**。タイトルが発言の引用なので、文字数下限や語の必須化は誤引用を作りうる（09-11 の判断を踏襲）。これも回帰テストで固定した。
2. **company-facts のキュー12件から絵文字を除去** → 適合率 **0%（0/12）→ 75%（9/12）**。
   - 残る3件は「実効20字未満」1件・「数字2個以上」2件で、再生成が要る。
3. 全体のキュー適合率 **24.2%（24/99）→ 33.3%（33/99）**。

コミット: `64f919b 指揮者 09-12: 切り抜き3chへゲート付与し、キュー補充をtitle_constraintsに通す`

---

## 4. 前回（09-11）施策の差分検証

| 施策 | 結果 |
|---|---|
| `min_effective_chars=20` の8ch適用 | **8ch すべてで生存（14時間後）** |
| akashic-librarian へのゲート新規付与 | 生存 |
| 答え提示語の必須化 | 生存 |
| fake-paper の接頭辞禁止 | 生存 |

### ✅ config 上書き事故は止まった

09-11 は**適用11分後**に 2ch-matome の `min_effective_chars` が別プロセスに消された。今回は**14時間後も全ch残存**。
前夜のコミット `32197ee` が `ChannelManager.patch_channel_file` / `set_section` に書き込みを集約し、**ディスクを土台に RLock + tmp/replace** するようにした効果とみられる。
→ **「config を変更したら数分おいて読み直す」という運用回避策は、当面は保険として残しつつ、事故は起きなくなったと判断してよい。**

### ❌ ゲートの効果そのものは測定不能

09-09 以降に公開できたのは4本だけで、**そのどれも analytics に載っていない**（analytics が 09-08 で止まっているため）。ゲート導入前後で比較できる実績が存在しない。
→ 公開済み動画を現在のゲートで測り直すと「55本中17本合格（30.9%）」だが、**これは公開当時ゲートが無かっただけで、効果判定には使えない**。レポートにもその旨を明記した。

---

## 5. 新しく分かった運用知見

- **`hard_constraints` は `title_rules` の**下**にある**（`d["title_rules"]["hard_constraints"]`）。トップレベルで探すと全ch「未設定」に見えて、**存在するゲートを消しにかかる事故**になる。本日一度踏みかけた。
- **`title_constraints` に channel を渡すときは `channel._raw`**。`ChannelProfile` 自体や `.data` を渡すと `is_enforced()` が常に False になり、**検査が静かに無効化**される（`generator._enforce_title_constraints` と同じ経路を使うこと）。
- **soffice の OOM は日によって違う。** 本日は1回目の `recalc.py` が成功（231数式・エラー0）したのに、作り直して2回目を回したら**2回とも exit 137 / 143 で死んだ**。
  → **最初から `reports/inject_cached_values.py` を使うほうが速い。** ただし `FormulaCache.put()` に渡す数式は**必ず先頭に `=` を付ける**こと。openpyxl は `=` 始まりの文字列だけを数式として扱うので、落とすとただの文字列セルになり**注入0件**になる（本日踏んだ）。
- **集計の突合は必ずやる。** 本日、キュー未公開の合計を **111件**と書いて commit まで通してしまった（正しくは **99件**、適合率も 21.6% ではなく **24.2%**）。xlsx を DB と突き合わせる検証工程で発見し、コード・テスト・レポートの全箇所を修正した。**手で足した数を根拠に使わない。**
- **前夜のコミット `32197ee` が追加した `test_fixes_20260912.py` は4件とも最初から赤。** 構造修正が未完了のまま入っている（clip の dirname / video_generator の sanitize / キュー全件ブロック時の選択 / 疑問形キューの消費順）。本日の作業では増減させていない。

---

## 6. 次にやること（優先順）

1. **OAuth 再認可**（要ホスト操作）。GCP OAuth 同意画面（project `844705815004`）を「テスト中」→「本番」へ公開してから全ch再認可する。テスト中のままだと**7日で必ず再発**する。**これが直るまで指揮者タスクは毎日同じ 09-08 データの再解釈しかできない。**
2. **`backend/.env` 18行目のコメントアウト解除**（1行）。切り抜き系は「上げられない」以前に「作れていない」枠がある。
3. `test_fixes_20260912.py` の赤4件を直す。
4. yokai-watch の `forbid_digits` を続けるか外すか — **実績が戻ってから**決める。今決めると二重適用になる。
5. `backend.log` のローテーション。

---

## 出力物

- `reports/youtube-analysis-2026-09-12.xlsx`（サマリ / チャンネル別詳細 / 直近動画一覧 / 改善提案 / 前回比較）
- `reports/orch_apply_20260912_result.json`
- `scripts/orch_apply_20260912.py` / `scripts/orch_xlsx_20260912.py`
- `backend/tests/test_fixes_20260912_orch.py`（27件・全green）
