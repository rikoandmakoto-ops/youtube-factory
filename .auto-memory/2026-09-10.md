# 2026-09-10 指揮者実行メモ

データ基準日: **2026-09-08**（新規データなし）

## 1. 今日いちばん重要な発見 — PDCAが2日前から空回りしている

`video_metrics` の最終日付が **09-08 で止まっている**。09-01〜09-08 は毎晩23:00台に296〜362行ずつ入っていたのに、**09-09 の夜間PDCAは0行**。`data/reports/2026-09-09/` はディレクトリだけ作られて中身が空（09-09 22:30 作成）。`channel_metrics` はさらに古く09-05で停止。

原因はほぼ確実に **OAuth トークン失効**。09-08 の `latest.md` の時点で、

- 完全失効4ch: `clip-animal` / `socio-rx` / `fake-paper` / `akashic-librarian`（`invalid_grant: Token has been expired or revoked`）
- 残り9ch: 残り約1.5日の警告 → ちょうど 09-10 前後に失効する計算

**恒久対策は既に判明している**: GCP の OAuth 同意画面を「テスト中」→「本番」に公開（project 844705815004）。テスト中のままだとリフレッシュトークンが7日で強制失効するため、何度再認可しても1週間ごとに同じ障害が再発する。これは 09-08 のレポートにも書かれていたが未対応のまま2日経過している。

**教訓**: 「要手動」とマークした基盤タスクは、自動実行では絶対に解消されない。放置すると分析の土台が消え、その上で行った施策の効果検証が全部できなくなる。09-09 の施策（評価予定日 09-16 / 09-20）は、このまま復旧しないと採否判断そのものが不能になる。

## 2. 収集経路が3つとも塞がっていた

無人スケジュール実行では「ブラウザでYouTubeから収集」が成立しなかった。

- 内蔵ブラウザ: youtube.com へのナビゲーションが拒否（サイト承認が必要／承認者不在）
- Chrome MCP: 拡張が3台接続されていて、どれを使うかユーザー選択が必須。無人では選べない
- サンドボックス直接fetch: youtube.com が許可リストに未登録

**教訓**: 無人実行のタスク定義で「ブラウザで収集」を前提にするなら、事前に (a) 対象ドメインの承認、(b) 使用するChromeを1台に固定、のどちらかを済ませておく必要がある。加えて今回は Linux ワークスペース自体も実行中にハングし、xlsx 生成と git コミットも不能になった。

## 3. 切り抜き3chが登録を1人も取れていない（実データ）

至上戦略が「登録者数の増加」である以上、これは無視できない。

| 系統 | 登録 | 再生 | 登録/1000再生 |
|---|---:|---:|---:|
| ゆっくり系6ch | 96 | 243,655 | **0.394** |
| 切り抜き3ch | 3 | 87,214 | **0.034** |

**11.5倍の開き。** 内訳は clip-lab 1登録/67,437再生、clip-kaneko 1/12,942、clip-fukada 1/6,835。

とくに `clip-lab` が象徴的で、**平均再生3,746.5は全11ch中1位**（2位 company-facts の2.9倍）、16,405 / 12,735 / 10,518 / 10,249 再生の単発ヒットを4本出しているのに、**登録/千は0.016で最下位**（scp-lab の1/43）。

**読み方**: 切り抜きは「他人の面白さ」で再生される。視聴者に「このチャンネルを登録する」動機が発生しない。再生数はリーチ指標としては優秀に見えるが、登録者増加にはまったく寄与していない。制作キャパを食っている分だけ純損失。

**次にやること**: データ復旧後、「登録/1000再生が30日で0.10未満なら投稿頻度を縮小」を判定基準として適用する。本日は新規データがないため実行を見送った。

## 4. 前回（09-09）施策の差分検証

### config 反映 = ✅ 全件確認できた

- `banned_words` に「秘密」→ scp-lab / daily-science / yokai-watch / pokemon-lab / company-facts / 2ch-matome の6ch全部で確認
- `forbid_patterns` に `(?:9\s*9|９\s*９)\s*[%％]` → 6ch全部で確認
- `hard_constraints.require_any_of`（答え提示語9語）→ 確認
- scp-lab の autopilot 枠: 6枠（同一時刻が2回ずつの重複バグ）→ 09:00 / 13:00 / 19:00 の3枠に是正済み
- scp-lab 17:00 → 09:00 の移動（17時はch内 n<2 でデータなし、09時は登録/千0.69・n=12）→ 確認

### 効果検証 = ❌ 実施不能

09-09 の施策は09-09以降の公開分に効くが、**09-09以降のデータが1行もない**。評価予定日（09-16 / 09-20）までにデータ収集を復旧させないと、施策の採否判断ができない。

## 5. 本日 config を変更しなかった判断

意図的に変更していない。09-10 に使えるデータは 09-08 スナップショットで、これは 09-09 の指揮者実行が既に判断材料として使い切った**同一データ**。同じ証拠で2回目の意思決定をすると、効果検証を経ないまま施策が二重適用され、どの変更が効いたのか永久に切り分けられなくなる。

`data/pdca-memory/applied_changes.json` には 08-16 と 08-19 に同じ状況で「新規の再生実績が無く根拠なし」として見送った前例が2件ある。今回もそれに従った。

**これを原則として INDEX.md に昇格させた**: 「新規の再生実績が無い日は config を変更しない」。

## 6. 未解決のまま持ち越した品質欠陥

- **scp-lab のテーマ重複**: 類似度1.00のペアが7組。SCP-1730（消えた施設・17年間・17名）だけで4本が完全重複。`theme_blacklist` に `17名` `17年間` `担当者17` は登録済みだが、**既に公開済みの重複本は残ったまま**。blacklist は将来の生成を止めるだけで、過去分は掃除されない。
- **company-facts の数値矛盾6件**: 同一企業・同一期で数字が食い違う（トヨタ年収 932/895/982万円、日本マクドナルド 670/576/400万円、日本郵政 695万円/60万円 など）。ファクト系チャンネルとしては信頼性に直結する。`fact_ledger` に矛盾検出ゲート（矛盾があれば公開ブロック）が必要。
- **daily-science**: 類似度1.00が1組（酸素が1秒だけ消えた）＋0.8以上が7組。

## 7. 補足で見えた事実

- `logs/backend.log` が **77MB** まで肥大化していて、grep するだけでコマンドがタイムアウトする。ローテーションが効いていない。障害調査の妨げになるので要対処。
- backend.log の末尾に、レンダリングが `70.51s/it` で残り `13:48:30` という異常に遅いジョブが記録されている。1本の動画生成に14時間かかる状態で、これが夜間PDCAの実行枠を圧迫している可能性がある。

---

# 【夜間追記】2026-09-10 23:00 nightly-full-progress

朝の指揮者実行（上記）のあと、22:30 に `daily-pdca-report` が**2日ぶりに走った**。
その結果と実DBを突き合わせて分かったこと。

## A. OAuth が 4ch → 13ch 全滅になった

朝の時点では「失効4ch＋残り9chが約1.5日で失効」だった。予告どおり **09-10 中に全部落ちた**。
22:30 のレポートは 13ch すべて `invalid_grant: Token has been expired or revoked.`。

結果、**12ch すべてが「登録者ソースが取得できないため判断保留」**、
**登録/1000再生は全ch算出不能**。至上指標が全ch測れない状態。

`video_metrics` は依然 **09-08 で停止**（09-09・09-10 ともに0行）。`channel_metrics` は 09-05 で停止。

## B. レンダは復旧した。詰まりは「アップロード」へ移った

これが今夜いちばん重要な訂正。09-09 の全進捗レポートは
「レンダが150〜800倍遅くなり投稿0本」と結論づけたが、**09-10 はレンダが正常に戻っている**。

| 日 | 生成 completed | YouTube 公開 |
|---|---:|---:|
| 09-08 | 24本 | **21本** |
| 09-09 | 20本（＋失敗2） | **2本** |
| 09-10 | **24本** | **2本** |

09-10 の公開2本は 01:15（2ch-matome）と 07:45（pokemon-lab）。
**07:45 以降は1本も上がっていない** — トークンが日中に順次失効した時刻と一致する。

> **教訓**: `job_queue.json` の `completed` は「レンダが終わった」だけを意味する。
> 公開されたかは `data/video_publish.db` を見ないと分からない。
> 09-09 の「投稿0本」も、実際にはレンダは進んでいた可能性が高く、
> 原因診断を1日ぶん誤らせた。**2つのソースを必ず突き合わせること。**

作った動画は `~/Desktop/動画出力用` と iCloud に出力済みで消えてはいない。
再認可すれば手動でも上げられる。

## C. 切り抜き4chは09-08を最後に公開ゼロ（2日間）

clip-lab / clip-fukada / clip-kaneko / clip-animal は **09-09・09-10 とも job_queue に1件も積まれていない**。
ゆっくり系8chは3枠ずつ積まれているので、切り抜きだけ別経路で止まっている。

素材取得だけは動いている（`clip_acquisition.json` に 09-09 3件 / 09-10 3件）。
**取れているのに作られていない**という09-09と同じ構図が続いている。

海外バイラル枠は `viral_translation_pending/` に **15件滞留**（08-31〜09-10、本日分 `viral_1wc95q5.json` 20:48）。
原因は `ANTHROPIC_API_KEY` 未設定で **10夜連続**。

## D. 本日 config を変更していない

朝の実行と同じ理由（新規の再生実績が0行）。加えて夜も新規データが入っていないため、
夜間タスク側でも変更しなかった。INDEX.md の鉄則どおり。

## E. 未対応のまま持ち越した基盤タスク（すべて「要手動」）

1. GCP OAuth 同意画面を「本番」に公開 → **3日連続で未対応**。これを直さない限り毎週再発する
2. 13ch の再認可（1 の後）
3. `backend/.env` 18行目のコメントアウトを外す（10夜連続）
4. YouTube 13ch の電話番号確認（サムネ403の唯一の解・10夜連続）
5. `logs/backend.log` のローテーション（79.7MB）
6. ホストのメモリ/swap（swap 96% / 空き72MB）

> **教訓の再確認**: 「要手動」とマークした基盤タスクは自動実行では絶対に解消されない。
> 3日放置した結果、失効4ch が 13ch 全滅になり、公開も1日21本→2本になった。
> 自動化された部分（レンダ・生成・素材取得）はむしろ正常に動いており、
> **止めているのは全部、人の手が要る1〜2行の設定である。**
