# MEMORY UPDATE 2026-09-04（指揮者タスク・朝）

実行: 2026-09-04 10:11–10:35 JST / 対象: youtube-factory のみ

---

## 1. 最優先: 制作が全面停止している（P0）

**2026-09-03 18:49 の `fake-paper / illustration` を最後に、OpenAI API が HTTP 429 を返し続けている。**
`data/api_usage.jsonl` の最終行がその 18:49。以降の成功記録はゼロ。

09-04 に APScheduler が自動発火した autopilot は**7件すべて失敗**した:

| 時刻(JST) | チャンネル | 失敗理由 |
|---|---|---|
| 06:15 | 2ch-matome | GPT failed to produce valid JSON after 2 attempts |
| 06:45 | daily-science | 同上 |
| 07:30 | company-facts | 同上 |
| 07:45 | pokemon-lab | 同上 |
| 08:00 | clip-kaneko | LLM フック生成失敗（OPENAI/ANTHROPIC キー） |
| 08:15 | scp-lab | GPT failed to produce valid JSON |
| 09:15 | akashic-librarian | 同上 |
| 09:30 | clip-animal | 全ての元動画が切り抜き済み（429とは別要因） |

直近ログの 429 出現は 89 回。リトライは 4 段（6s→16s→46s）だが全段で 429。
**本日の公開本数は 0 本。** 09-03 20:30 / 20:45 の clip-kaneko / clip-lab も同因で失敗しており、実質 09-03 夜から止まっている。

> 復旧すれば、残りスロット（妖怪 11:15 / 2ch 11:30 / 1分科学 11:45 / SCP 12:15 / 企業ホンネ 12:45 / ポケモン 14:15 / 架空論文 14:45 …）から**本日更新した新コンフィグで自動的に走る**。手動トリガーは不要。

### 併発している欠損
- **`ANTHROPIC_API_KEY` 未設定**（継続）。clip-kaneko / clip-lab が毎日中止。加えて `success_patterns.json` の `gpt_insights` が**全ch `null`**（理由 `"ANTHROPIC_API_KEY 未設定"`）で、分析の質が落ちている。
- **`channel_metrics` が 2026-08-31 で停止**（4日欠測）。YouTube Analytics API 由来の日次値が取れておらず、登録者の日次増減が追えない。`video_metrics`（累計）は 09-03 23:25 まで取れている。
- **DB の `impressions` が過少**。Shorts のフィード表示が乗らず views ≫ impressions となり、CTR が 30% 超で頻出する。**CTR は絶対値として使えない**ので、本日の判断からは外した。

---

## 2. 実績（08-31 → 09-03 スナップショット差分）

全体 Δ再生 126,857 / Δ登録 50 → **登録/千再生 0.394**

| 層 | チャンネル（登録/千再生） |
|---|---|
| 上位 | アカシック 0.83 / SCP 0.79 / 企業ホンネ 0.76 |
| 中位 | 妖怪 0.66 / 1分科学 1.42※ |
| 下位 | 2chまとめ 0.31 / ポケモン 0.30 / 切り抜き深田 0.19 / 切り抜き金子 0.17 |
| 転換ゼロ | **切り抜きLab 0.00（Δ再生 28,995）** / 架空論文 0.00 |

※ 1分科学 1.42 は Δ再生が 2,115 と小さいため分母が効いており、水準としては中位。

- `video_metrics` は**累計値**（同一動画の 09-01→09-03 が 3576→3597）。累計は減らないので、負の差分は取得欠損として 0 に丸めた。
- 09-03 の取得で **5件が views=0** として記録されていた（1分科学3 / 2ch 1 / ポケモン1 / SCP 1）。これらは増分計算から除外。
- 単発の勝ち: 切り抜き深田「"時間停止"で使うスタジオ」Δ15,579 / 切り抜きLab「SNSをやる人は天才じゃない」Δ12,732。**いずれも登録 0〜3。再生は取れるが登録に落ちない構造は変わっていない。**

---

## 3. タイトル要素の再検証（チャンネル内対照・n=237）

母集団: `date=2026-09-03` / `published_at>=2026-07-05` / 累計100再生以上。
各chで「あり／なし」を比較し、**両方2本以上あるchのみ**集計してチャンネル間交絡を除去した（09-03 に確立した方法を踏襲）。

| 要素 | あり | なし | 効果量 | 対照ch | 09-03 の値 | 判定 |
|---|---|---|---|---|---|---|
| 「正体」 | 0.69 | 0.46 | **1.50倍** | 3（全て同方向） | 1.53倍 | **再現。毎バッチ1本必須を継続** |
| 数字＋単位 | 0.55 | 0.33 | **1.65倍** | 7（5ch同方向） | 1.56倍 | **再現。全タイトル必須を継続** |
| 疑問符「？」 | 0.37 | 0.50 | **0.75倍（負）** | 5 | 0.62倍 | **再現。08-23 の必須化撤回を追認** |
| 先頭絵文字 | 0.60 | 0.40 | 見かけ1.50倍 | 2 | 1.03倍 | **不採用**。ch内では 2ch 0.00vs0.19・企業ホンネ 0.69vs0.92 と**両ch負**。Simpson の逆転 |
| #shorts 形式 | 0.55 | 0.44 | 1.25倍 | 8（6ch同方向） | （新規） | **変更不要**（下記） |

**「#shorts 形式が有利」は見かけだった。** 「【ショート】」は `api_phase4` のフォールバック経路（`short_d.get("title") or job.title+"【ショート】"`）に落ちた動画に付く文字列で、タイトル形式ではなく**短編タイトル生成の失敗**を表していた。この不具合は **09-01 の `video_generator.py` 修正で既に解消**しており、09-03 公開分は全12本が正しい `#shorts` 形式。追加の変更は不要。

---

## 4. 本日実施した変更

### 4-1. テーマキューに「既に撤回済みの負けパターン」が残っていた（最大の発見）

| ch | 状態 | 内容 |
|---|---|---|
| **pokemon-lab** | 7/7件が該当 | 全件が『〜に隠された秘密／設定／メッセージ』。**08-31 実測で登録0.47倍・維持率 29.6% vs 35.8% と劣後が確認され撤回済み**のテンプレ。キューは **07-30 補充のまま**で更新されていなかった |
| **yokai-watch** | 8/10件が該当 | 同上。本chの `title_style` は既に「テンプレ型 維持率30.1% vs 自然文53.6%」と明記していたのに**キューが未同期** |
| **2ch-matome** | 10/10件が違反 | 全件 32〜39字。本chの `title_style` は**「全角12〜28字厳守」**。さらに全件が修羅場／スカッと型で、`theme_priority` が最優先と定める**参加型・大喜利が0件** |
| **company-facts** | 8/9件が違反 | 30字超（`title_rules` 30字以内）かつランキング／総解説型。実測の勝ち筋は**個社名×年収実額×「正体／裏側」**（トヨタ Δ3,597 / 大谷 Δ2,239 / 三菱商事 Δ1,970） |

> **方針は正しく更新されていたのに、キューだけが古い方針のまま残り続けていた。** 放置すれば今後2〜3日分が既知の負けパターンで生産される状態だった。

**対応**: 4ch のキューを全入替（pokemon 10 / yokai 10 / 企業ホンネ 12 / 2ch 12 = 44件）。
全件が「正体」「数字＋単位」を含み、疑問符ゼロ、字数規定内であることを機械検証済み。
pokemon / yokai は `title_rules.forbid_patterns` に `に隠された(秘密|設定|背景設定|メッセージ|由来)` を追加し、**backend が実際に読む `theme_priority.title_style` 側にも同内容を追記**した（`title_rules` は backend が読まない死に設定＝09-03 判明済み）。

### 4-2. cta_position の A/B を開始

`cta_position=end` 群（アカシック 0.83 / 企業ホンネ 0.76 / 1分科学 0.60、群平均 0.73）は
`after_hook` 群（2ch 0.17 / ポケモン 0.23 / 妖怪 0.23 / 架空論文 0.00、例外 SCP 0.79、群平均 0.28）を上回る。

ただし**チャンネル間対照で交絡を含む**（bak を全て遡ったが、どのchも cta_position を一度も変更しておらず ch内対照が取れない）。断定せず A/B として:

- **変更アーム**: yokai-watch / pokemon-lab / 2ch-matome → `after_hook` → **`end`**
- **対照群**: scp-lab / fake-paper は `after_hook` のまま**据え置き**
- 各config に `content_policy.cta_position_experiment` として baseline・仮説・反証条件を記録

**反証条件: 2026-09-11 に3chの登録/千再生を再測定し、baseline（妖怪0.23 / ポケモン0.23 / 2ch 0.17）を上回らなければ `after_hook` に戻す。**

---

## 5. 検討したが実施しなかったもの

- **2ch-matome の参加型タイトル全面移行**: 維持率では参加型 62.8% 対 説明型 35.6% と圧勝だが、**判断軸の登録/千再生では 参加型 0.14 対 説明型 0.22 で逆転**する。`optimization._comment` が指摘する「維持率と登録転換の符号がchごとに逆」の実例。維持率を根拠に動かすべきでないと判断。
- **冒頭離脱対策のシナリオ変更**: intro バケットが12ch中8chで最大の離脱（架空論文 0.0264 / 企業ホンネ 0.0245 / ポケモン 0.0228。離脱点は ratio 0.10〜0.14＝2〜3行目で説明が始まる位置）。だが**維持率は `reference_only_metrics`** であり施策の採否に使わないと 09-04 に決定済み。cta_position A/B の結果が出る 09-11 まで変更を重ねず交絡を避ける。
- **バックエンドへの制作再トリガー**: APScheduler が既に全chを自動発火済みで、失敗原因はトリガー不足ではなく 429。再送は同じ失敗を繰り返し残クォータを消費するだけ。

---

## 6. 生成物

- `reports/youtube_analysis_20260904.xlsx`（5シート: サマリー / チャンネル別詳細 / 動画別パフォーマンス / 改善アクション / トレンド。数式105セル、DB直読みとの独立検算で全行一致を確認）
- `MEMORY_UPDATE_20260904.md`（本ファイル）
- config バックアップ: `*.bak_pdca_20260904_orch`（4ch + theme_queue 4件）

---

## 7. 明日以降の申し送り

1. **OpenAI 429 の解消が全ての前提。** 解消するまで何本コンフィグを直しても世に出ない。
2. **09-11 に cta_position A/B を判定する**（変更3ch vs 据え置き2ch）。
3. `ANTHROPIC_API_KEY` を入れると `success_patterns.gpt_insights` が全chで復活し、分析の解像度が上がる。
4. `channel_metrics` の取得復旧（4日欠測中）。
5. **clip-lab は Δ再生 28,995 で全ch最大なのに登録0。** 凍結解除の前に登録導線の設計が要る。
6. 今回の教訓: **「方針を更新したらキューも同時に更新されているか」を毎回確認する。** 4ch中4chでキューが方針から取り残されていた。
