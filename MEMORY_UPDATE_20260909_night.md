# メモリ更新差分 — 2026-09-09 深夜（nightly-full-progress）

実行: 2026-09-09 23:09 JST 開始

> ⛔ `~/.auto-memory/` は **13夜連続で接続フォルダ外**。接続済みは
> `Developer` / `youtube-factory` / `iCloud Documents` / `Downloads` の4つのみ。
> 本ファイルと `HANDOFF.md` / `MEMORY_UPDATE_*.md` をメモリ本体として扱う。

> ⛔ 本タスクのサンドボックスは **bash 自体が起動しなかった**（`Shell 'bash' not found in PATH`）。
> sqlite / git / curl は一切使えず、**Read / Glob / Grep によるファイル直読みのみ**で判定した。
> したがって「作業ツリーの未コミット件数」「video_publish.db の実投稿行」は本日**未確認**。
> 投稿0件の判定は `logs/backend.log` を根拠にしている。

---

## 0. 本日の結論（3行）

1. 🚨🚨 **09-09 の YouTube 投稿は 0 本。** 09-08 の21本から全滅した。
   原因は API でも OAuth でもサムネでもなく、**動画レンダリングが約150〜800倍遅くなったこと**。
   ジョブは22件投入されて全部キューに積まれたまま日付が変わった。（→ §1）
2. 🚨 **06:15 の再起動は 09-09 午前の設定変更より前**だった。
   したがって **`scp-lab` の枠重複も、`2ch-matome` の 07:00→09:00 移設も、稼働プロセスには入っていない。**
   ディスク上の JSON と APScheduler の登録内容が乖離している。（→ §2）
3. **`daily-pdca-report` が 09-09 に実行されていない。**
   `lastRunAt` が 09-08 23:33 のまま、`nextRunAt` が 09-10 へ飛んでいる。
   `success_patterns.json` / `retention_insights.json` は 09-08 23:20 で止まっている。（→ §5）

---

## 1. 🚨🚨 レンダリングが約150〜800倍遅くなり、本日の投稿が全滅した

### 実測（`logs/backend.log`）

| 時点 | 実測値 |
|---|---|
| **09-08（再起動前）** | `frame_index: 38%\|474/1233 [00:58<02:37, **4.83it/s**]` ＝ **毎秒4.83フレーム** |
| **09-09（再起動後）** | `frame_index: 2%\|12/695 [07:57<8:27:28, **44.58s/it**]` |
| 同・末尾（23:45時点） | `1200/1271 [**11:21:08**<23:40, 20.00s/it]` / `590/695 [**16:51:14**<2:17:38, **78.66s/it**]` |
| 最悪値 | `374/695 [9:44:04<14:27:45, **162.20s/it**]` |

- 1フレーム 0.2秒 → **20〜162秒**。同一パイプラインで桁が3つ違う。
- ワーカーは2本。**その2本が11時間21分／16時間51分のレンダに占有されたまま日付をまたいだ。**
- 結果、**09-09 のアップロード完了は 0 件**（`✅ アップロード完了` の最終出現は 09-08 20:30 の `v=NQAeWaY94a8` / clip-kaneko）。
- `📥 Job queued` は 09-09 中に **22件**（`652750`〜`656144` 行）。全件が未処理のまま滞留。

### 原因の推定（※未確定・要検証）

- 06:15 頃の再起動で **09-09 01:22 のコミット `fix: 09-08 深夜PDCAの指摘6件を修正する` のコードが初めてロードされた。**
  この回で `backend/pipeline/video_generator.py` と `backend/pipeline/pillow_illustration.py` が変更されている。
- 遅くなったのは**再起動の直後から**で、それ以前（09-08）は正常速度だった。時系列は一致する。
- `video_generator.py` は `make_frame(t)` の中から `pillow_illustration.generate_pillow_illustration()` を呼ぶ経路を持つ。
  **フレーム単位で画像生成が走るようになっていれば、この桁の悪化を説明できる。**
- ★ ただし **コード差分は本タスクでは読めていない（git 不可）。上記はあくまで推定。**

### ★ 明日の最優先アクション

1. `git show` で 09-09 01:22 コミットの `video_generator.py` / `pillow_illustration.py` の差分を読む。
2. フレームごとの画像生成／キャッシュ破棄が入っていないか確認する。
3. 直らないなら **このコミットの当該2ファイルだけ revert してから再起動する。**
4. 溜まった22件のジョブは、そのままだと再起動で消える（§4）。

---

## 2. 🚨 06:15 の再起動は「設定変更より前」だった — 稼働中の枠は旧設定のまま

`logs/backend.log` 652,596行の再起動直後に登録された枠:

```
📅 Autopilot scheduled for 2ch-matome [slot 0]: 07:00 JST → next run 2026-09-10T06:15
```

一方、ディスク上の `data/channels/2ch-matome.json` は **09:00 / 12:15 / 21:00**。
09-09 午前（10:38）の指揮者の変更は、**APScheduler には一切入っていない。**

### 乖離が確定しているもの

| ch | JSON（ディスク） | APScheduler（稼働中） |
|---|---|---|
| 2ch-matome | 09:00 / 12:15 / 21:00 | **07:00** / 12:15 / 21:00 |
| scp-lab | 09:00 / 13:00 / 19:00（3枠） | **旧6枠のままの可能性が高い**（idx 3,4,5 が残存） |
| pokemon-lab | 08:30 / 15:00 / 17:00 | 19:00 が残っている可能性 |

- 09-09 の scp-lab の発火は**2回**しか観測されていない（654397 / 655564 行）。
  6本作っていた形跡は**本日は無い**が、これはキューが詰まって発火自体が減った可能性もあり **切り分け不能**。
- ★ **結論は変わらない: 設定変更のあとに再起動していないので、稼働プロセスは旧設定で動いている。**

### 設定の残存確認（09-09 午前の変更）

`data/channels/*.json` を全13ch 直読みした結果、**午前の変更はディスク上に全て残っている**（バックエンドによる上書き消失は再発していない）。

| ch | `banned_words:"秘密"` | 99%型禁止 | 個別追加 | `require_any_of` |
|---|---|---|---|---|
| daily-science | ✅ | ✅ | — | ✅ |
| scp-lab | ✅ | ✅ | `—\s*.*【` ✅ | ✅ |
| 2ch-matome | ✅ | ✅ | `ワイ` / `質問ある` ✅ | ✅ |
| pokemon-lab | ✅ | ✅ | — | ✅ |
| yokai-watch | ✅ | ✅ | 末尾疑問符禁止 ✅ | ✅ |
| company-facts | ✅ | ✅ | — | ✅ |

- 効き始めた時刻も実測できた: **06:15 枠では「ワイ」も「秘密」も素通り**（`ワイの財布から毎月消える謎の正体が判明したｗ`）、
  **11:30 枠で「ワイ」ゲート発動**、**17:15 以降で「答え提示語が必須」発動**。
  → **設定は午前の反映以降の枠からのみ有効。09-10 が初の全数判定日。**
- `fake-paper` / `akashic-librarian` / `socio-rx` / 切り抜き4ch には `title_rules` が無く、09-09 の改善は**及んでいない**。

---

## 3. 🆕 `job_queue.json` の永続化が壊れている

```
⚠️ JobQueue persist failed: [Errno 2] No such file or directory:
   '.../data/job_queue.json.tmp' -> '.../data/job_queue.json'
```

- ログ全体で **78回**。`data/job_queue.json` 本体は存在する。
- 一時ファイル → 本体の `rename` が失敗している。**書き出し先ディレクトリまたは tmp の作成に失敗している**（推定）。
- 実害: **キューがディスクに保存されない。再起動すると滞留中の22件は消える。**
- ★ §1 の再起動を行う前に、**この22件を捨てる前提で動くか、先に永続化を直すかを決めること。**

---

## 4. その他 09-09 の実測

- **ERROR レベルのログ: 0件**（09-09 領域）。壊れ方が「エラーではなく遅い」なので検知されなかった。
- **サムネ 403: 0回**。アップロードが0件だから発生していないだけ。**本人確認は依然として未了で、9夜連続の未解決。**
- **Autopilot 発火 32回 / 失敗 10件**:
  - clip-kaneko 3（LLMフック生成失敗＝`ANTHROPIC_API_KEY`/`OPENAI_API_KEY`）— 09-08 に0件へ解消したはずが**再発**
  - clip-fukada 2（ffmpeg **1800秒タイムアウト**）
  - clip-lab 3（ffmpeg 1800秒TO / yt-dlp字幕 300秒TO / `ANTHROPIC_API_KEY` 未設定）
  - clip-animal 2（yt-dlp 300秒TO / 切り抜ける元動画なし）
  - ★ **ffmpeg の1800秒タイムアウトは §1 の遅延と同根の可能性が高い。**
- **切り抜き素材の取得は動いている**: `clip_acquisition.json` に 09-09 の 12:53 / 17:52 / 20:07 の3件。
  **取れているのに作れていない。**
- `ANTHROPIC_API_KEY` 未設定は **9夜連続**（`backend/.env` 18行目コメントアウト）。

---

## 5. 定期タスクの実行状況（本日）

| タスク | cron | lastRunAt | 判定 |
|---|---|---|---|
| youtube-orchestrator | 10:03 | 09-09 10:03 | ✅ 実行（ネットワーク不可でPhase1/4は未実行） |
| daily-youtube-analysis | 10:10 | 09-09 10:10 | ✅ |
| clip-lab-method-reminder | 3日おき10:00 | 09-07 | 次回 09-10 |
| daily-merge-all-projects | 22:05 | 09-09 22:05 | ✅ 実行（push は認証不可で未達） |
| daily-project-handoff | 23:10 | 09-09 23:09 | ✅ |
| nightly-full-progress | 23:09 | 09-09 23:09 | ✅（本タスク） |
| **daily-pdca-report** | **23:30** | **09-08 23:33** | 🚨 **09-09 は未実行。`nextRunAt` が 09-10 23:33 へ飛んでいる** |
| vercel-migration-reminder | 23:10 | 09-08 23:09 | ⚠️ 同様に 09-09 分が飛んでいる |

- HANDOFF に「PDCA 起動を 23:00 → **22:30** に前倒し」とあるが、
  **Cowork 側の `daily-pdca-report` の cron は `30 23 * * *`（23:30）のまま**。前倒しは反映されていない。
- `success_patterns.json` / `retention_insights.json` の `generated_at` は **09-08 23:20 が最新**。09-09 の更新なし。
- `cross_channel_keywords.json` は `"date": "2026-09-09"` ✅（指揮者が書いた分）。

---

## 6. コンフィグ変更の記録（09-09 に確定したもの）

以下は 09-09 午前の指揮者が入れ、**深夜時点でディスク上に残存を確認済み**。次の指揮者は再調査不要。

- 全6ch: `banned_words` に `"秘密"` を追加（登録/千再生 0.120 vs 0.527 ＝ 4.4倍差）
- 全6ch: 99%型フックを `forbid_patterns` で禁止
- 全6ch: `hard_constraints.require_any_of`（答え提示語）を追加
- 2ch-matome: `(?<![ハスロ])ワイ(?![ドヤンルフパブザ])` と `質問ある` を禁止
- scp-lab: `—\s*.*【`（記録票フォーマット）を禁止 ＋ 枠を3つへ重複解消 ＋ 17:00→09:00
- 2ch-matome: 07:00→09:00 / pokemon-lab: 19:00→15:00
- yokai-watch: 末尾疑問符を禁止
- pokemon-lab: `theme_seeds` 23→33本
- daily-science: `theme_priority.viral_hooks` から「99%が知らない」系を削除
- `repair_with` の割当: daily-science=正体 / scp-lab=真相・裏側・実態 / pokemon-lab=真相 /
  yokai-watch=正体・わけ・理由 / 2ch-matome=理由・わけ / company-facts=実態・裏側

**★ ただし §2 のとおり、投稿枠の変更は稼働プロセスに反映されていない。**

---

## 7. 明日（09-10）の指揮者がまず見るべきもの

1. **レンダリング速度が戻ったか。** `logs/backend.log` の `it/s` を見る。戻っていなければ §1 の revert。
2. **投稿が再開したか。** 09-09 は 0 本。09-08 は 21 本。
3. **再起動されたか。** されていなければ §2 の枠乖離が続く。
4. **22件のジョブが処理されたか、消えたか**（§3）。
5. **09-10 は「秘密」「ワイ」「答え提示語」が全数に効く初日** — ただし投稿が動いていることが前提。
6. `daily-pdca-report` が 09-10 に実行されたか。2日連続で飛んだら cron を見直す。

---

## 8. 本タスクで行った書き込み

- `MEMORY_UPDATE_20260909_night.md`（本ファイル・新規）
- `data/reports/全進捗_2026-09-09.md`（新規）

他プロジェクトのコード・設定・git には**一切触れていない。読み取りのみ**。
